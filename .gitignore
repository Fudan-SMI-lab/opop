.venv/
runs/
# Abandoned first location for the GLM arm's run dir; the arm now writes to
# ../v2-glm/runs (see configs/experiments_l3_glm.yaml). Ignored rather than merely
# deleted because sandbox opencode.json files under any run dir can now carry a
# provider block WITH its apiKey — `sandbox_config_path` copies it in.
runs-glm/
__pycache__/
*.pyc
.pytest_cache/
uv.lock
# workspace / tooling artifacts that must never be committed
.omo/
.codegraph/
.triton-cache-wsl/
D:/
*.log
opencode-server.log
state.json
# opencode config can carry plaintext API keys — never commit
.opencode/
opencode.jsonc
opencode_backup.jsonc
kimi-provider.yaml

# monitor dedup scratch files
.tmp_*
