# v3 实施前的缺口清单(2026-09-10 复审)

> **⚠ 已被取代（2026-09-10)**：用户对设计提出三条纠正后，本文档的阶段表与判据表已不再是当前版本。
> **当前有效版本是 `docs/v3-revised-design-and-implementation.md`**，其中：S2c 整条删除、S4 的静态估计器删除、
> 新增 S2d（动作—资源预期与对账账本）与 S0/G32。本文档保留作「四条硬前置如何被关闭」的记录 —— 那四条**全部已关闭**。

**这份文档回答一个问题:现在能不能开始写 S2?**

结论:**不能立刻开始。有 4 条硬前置未完成,其中 3 条是今天实测新发现的。** 但它们全部**便宜且修法明确**,不是设计问题。

**方法**:每条结论都来自当前 checkout 的代码复验或当天的实测,不是读方案推断。凡是「没有」的,都写出**我做了哪个检索**。

---

## 一、一句话总览

| | |
|---|---|
| **v3 已实施的** | **全部属于 G 清单**(G1–G25:对 v2 已有量的**测量正确性**修复) |
| **v3 方案的 S 阶段** | **S1 / S1b / S2 本体 / S2b / S2c / S3 / S4a / S6 —— 零代码** |
| **唯一有代码的 S 项** | S2 的**周边验收条件**(J2-8 的 L2 修法 a、A-4 替代自检、B-2 显存维度)+ S4b + S5 的第一步 |
| **而 S4b 的代码** | **恒为空转** —— 两个 profile 参数永远是 `None`(G27,今天实测) |

**所以「资源维度分析、资源间转化效率、资源→性能转化效率」这三项:第一项部分有(作为量,不是作为按维度判决),后两项没有真实数据。**

---

## 二、S 阶段实施状态

| 阶段 | 应该做什么 | 状态 | 代码证据 |
|---|---|---|---|
| **S1** | 编译期可知的不可行**从空间里声明掉**(依赖域收缩) | **无代码** | `tpe.py:51-78` 仍从完整 `space.domains` 采样,靠 `guard_ok`(`orchestrator.py:1029`)**抽出再拒**;`paramspace/` 无域收缩层;全仓 0 处 `early_config_prune`。现有的编译期预筛 `_prescreen_space`(`orchestrator.py:1130`)**来自 v2**(`b7ebdd6`/`dabd3b9`,`git branch --contains` 判定在 v2);方案要求的零 GPU 离线重放脚本 `replay_sampler.py` **不存在** |
| **S1b** | 取值级别的**无条件失败降权**(非黑名单) | **无代码** | `tpe.py:119-121` 与方案描述的现状逐字相同;无逐取值统计、无采样概率重加权、无开关。离线工具存在但 harness 不 import(`scripts/retire_risk.py`、`scripts/value_conditional.py`) |
| **S2** | 单标签 → **按维度独立的多份判决** + 消化层 + 替代自检 | **部分(本体缺失)** | **缺**:`classify()` 返回**单个** `BottleneckVerdict`(`bottleneck.py:223-256`),`kind: str` 一个字符串(`:206`),9 个 return 点各给一个标签;无 `{dimension_id, measured, ceiling, ceiling_provenance, applicable, confidence}`;无 `{dimension, severity, root_cause, ranked_recommendation, expected_delta}` 四元组;**无 `label\|vector` 开关**(grep `diagnosis.mode\|per_dimension\|applicable` → 无匹配;`config.py` 里根本没有 v3 段)。**有**:J2-8 的修法 a(`bottleneck.py:520-530`)、>100% clamp(`:544-568`)、逐精度+逐后端天花板(`:138-193`)、G5 余量作为量(`:295-300`)、显存峰值维度、A-4 替代自检已接线(`reading_checks.py` → `profilerx.py:95-97`) |
| **S2b** | 访存模式墙(静态坐标) | **无代码** | grep `coalesc\|bank_conflict\|access_pattern\|stride` → 唯一 `stride` 命中是 `tritonmm.py:160`(标定 matmul 的参数)。原料已采集却无人读:`statics.py:66-77` 数了 STS/LDS/BAR/vec_128,而 `SassCounts.vectorized_frac`(`:151`)在 `src` 里**零调用者** |
| **S2c** | θ_best ±1 步**邻域局部资源地图** | **无代码** | grep `neighbou?rhood\|resource_map\|local_map` → 无。`_prescreen_space` 只记 `{configs_probed, infeasible}`(`orchestrator.py:1192`),不记任何逐配置资源读数;`structural_signature`(`families.py:79`)只用于族去重。测量能力只存在于离线探针(`scripts/probe_resource_map.py` 等) |
| **S3** | 每维下界 + 天花板出处 | **部分(近乎无)** | 只有任务级下界/上界(`task_cost.py:17-26`)与容量余量(`bottleneck.py:295-300`);其余维度无下界;**任何判决上都没有 `ceiling_provenance` 字段**。`Performance-Score` 确实全仓无踪 —— 但那是**删除**,不是实现 |
| **S4** | (a) 静态估计器作排序先验;(b) 成对实测转化效率 | **部分,且实施的那半是空转** | (a) **无**:grep `dQ\|delta_Q\|warm_start` → 无;扩展排序仍按边界/失败率(`orchestrator.py:1474`)。(b) **写了但没人读,且收不到数据**:`conversion.py` 形状正确(逐维度带极性 `:37-48`、拒绝跨维 argmax `:21-26`),但**唯一调用点的两个 profile 参数恒为 `None`**(G27),且输出只进 `FAMILY_ROUND_RECORDED` 事件而**三个消费者只读 `best_ms`/`family_id`**;`report.py` 里 `conversion` **0 次命中** |
| **S5** | 候选真实流量 | **部分——只有使能的第一步** | `threads_launched`/`launches` 已实测(`worker_main.py:295-317`、`models/core.py:274-281`)并进了判据(`orchestrator.py:1443`);**真实流量本身仍是上下界对**(`candidate_aten_bytes` 下界 vs `reference_bytes` 上界),不是实测值 |
| **S6** | 通信维度 | **无代码** | grep `nccl\|all_reduce\|communication\|multi_gpu\|world_size` → 除 `modules.py:567` 一句 warp-shuffle 散文外无 |

---

## 三、开始 S2 编码前必须先解决的 4 条硬前置

| # | 前置 | 为什么是**硬**前置 | 成本 | 状态 |
|---|---|---|---|---|
| **P1** | **G26:判据仍误判维度**(J2-8,A800 实测 **2/5** 误判) | S2 的产物是**按维度并列的多份判决**。**判据误判维度时,并列输出只是把同一个错误分成两栏** —— 这是 S2 自己写下的硬门 | 小(`l2_bytes` 字段已存在,只是没被读) | ⏳ **未做** |
| **P2** | **G27:转化效率恒为空转**(profile 恒 `None`) | S4 的地基。**现在关于「资源→性能转化」我们一条真实数据都没有**;S2 的记录骨架也要靠它 | 小(`update_best` 带上 profile) | ⏳ **未做**(今天实测发现) |
| **P3** | **G9 的判据 J2-5**(终局不变差) | **J2-5 与 G9 是同一个问题**:改反馈内容会不会让结果变差。外部有反向先例(few-shot 范例把 fast_1 从 10% 降到 6%)。**S2 改的正是反馈内容** | 中(重跑,已就绪) | 🔧 **被 G24 中断,已修待重跑** |
| **P4** | **J2-2b:替代自检**(撤回 TMA 闭合留下的空缺) | 方案明写「**S2 的验收判据必须包含"替代自检已确定并实现",否则 S2 不算通过**」 | **已完成** | ✅ `reading_checks.py` 已接线 |

**另外 3 条是 S2 编码时的一部分,不是前置**:G28(原始 evidence 进了 prompt —— 消化层就是解决它)、`label\|vector` 开关、四元组格式。

---

## 四、方案自己定的验收判据:哪些能现在验、哪些要 GPU

| 判据 | 内容 | 现状 |
|---|---|---|
| **J1-1..4** | S1 的死点消失 / 有效评估数上升 / 早期质量 / 终局不变差 | **要 2×12h 对照 run**;且**离线重放脚本尚不存在** |
| **J2-1** | 区分度上升(基线:L3:21 的 19/20 同标签) | 要 S2 代码 |
| **J2-2b** | 替代自检已实现(N1 必败测试) | ✅ 已实现,**但 N1 那两个必败测试尚未写** |
| **J2-3** | 原始向量不进 prompt(N2 必败测试) | ❌ 当前**相反**(G28) |
| **J2-4** | 不可达屋顶被标出(L3:48 张量核) | 要 S2 代码 |
| **J2-6** | 多维同时 binding 时输出多份 | 要 S2 代码 |
| **J2-7** | 改写后资源被**重测**而非继承 | 要 S2 代码;与 G27 同源 |
| **J2-8** | **L2 内候选不得报 DRAM 绑定**(N4 必败测试) | ❌ **2/5 误判**(P1) |
| **J2-5** | **终局不变差** | 🔧 = G9(P3) |
| **J2c-1..5** | S2c 的成本/非常数/不复用/查表胜过预测/终局 | 前提已实测;**J2c-4 可零 GPU 用历史数据验** |
| **N1/N2/N3/N4** | 四条**必须会失败**的测试 | **四条全部未写** |

**关键路径不是编码,是对照 run。** 方案自己写着:S1 与 S2 各需一个对照 run(开/关 × 12h)且**不可合并**,所以**验收是关键路径**。

---

## 五、按依赖排序的下一步

1. **P1(G26)+ P2(G27)** —— 两条都小、都明确、都在 S2/S4 的地基上。**先做这两条。**
2. **N1/N4 两条必败测试** —— 顺手,且它们正是 P1/P2 的验收判据。
3. **P3(G9 重跑)** —— G24 已修,可以重跑;它同时就是 J2-5。
4. **然后才开始 S2 编码**,并同时处理 G28 与 `label|vector` 开关。
5. S1 可与 S2 并行开发(方案允许),但**上线必须串行**,且要先补 `replay_sampler.py` 拿零 GPU 结论。

---

## 六、今天新发现的 3 条(都不是设计问题,是实施细节)

| # | 发现方式 | 性质 |
|---|---|---|
| **G24** | G9 重跑在**真正装了 G20 的机器**上跑 | **我自己的 G20 修复误杀 5/6 合法调用**。前提「在工作的 agent 会改沙箱」对推理模型是假的 —— glm-5.3 在服务端思考,不落本地文件。已修:判据补上「服务端仍在工作」这一半 |
| **G25** | 同一次实测 | 被上次 abort 关掉的传输抛**裸 `RuntimeError`**,不是 `AgentCallError` ⇒ 逃出重试循环,丢 3 个臂。已修:发请求前自愈重开 |
| **G27** | 复核审计结论时实测 | **G3 恒为空转**,`resource_deltas` 从未产生。守卫测试断言的是**源码字符串**,而它的失败信息恰好描述了正在发生的事 |

**三条共同的形态**:都是「**不报错、只给错结果**」,而且都只有在**真实运行**里才暴露 —— 与 v2 反复记录的教训同族。
