# 移交外部 agent 的 prompt(第二轮):对反馈的答复与下一步

> 用法:把 `=== PROMPT 开始 ===` 与 `=== PROMPT 结束 ===` 之间的全文,连同附件一起提交。
>
> **附件(按重要性排序)**
> 1. `docs/reply-to-feedback-review-research-v5.md` —— 逐点答复正文(本 prompt 的证据来源)
> 2. `docs/audit-p2prime-reader-defects.md` —— P2′ 三项缺陷的实测审计,四定义对照表
> 3. `docs/window1-results.md` —— 窗口 1 完整读数(§4 与 §6 已按本轮更正重写)
> 4. `scripts/probes/v41_p2prime.py` —— 修好的读数器(commit `bd65af0`)
> 5. `tests/test_v41_p2prime_reader.py` —— 11 个已知答案正对照
> 6. `scripts/probes/v41_profile_coverage.py` —— 交付物 4 的探针
> 7. `docs/feedback-review-research-v5.md` —— 你上一轮的反馈(被答复对象)

---

=== PROMPT 开始 ===

你上一轮对 `docs/research-v5-unified-objective-framework.md` 的外部评审
(`docs/feedback-review-research-v5.md`)写得很好。**你带行号的技术指控我逐条去核实了,
不是辩论——而其中一条推翻了我自己的结论,那是本轮最有价值的反馈。**

本 prompt 分四部分:①你说对了什么(含我因此撤回的三处);②你的两处推论需按实测更正;
③你要的五项交付物的完成情况;④下一步。

**判读纪律(与上一轮相同,违反会被退回)**:只信盘上 `events.jsonl`;
终值只在 `RUN_FINISHED.summary.best`;不跨机器/跨任务并表(box4 是 8352V、box1 是 8358P,
compile_s 差 32%);对等性按 GPU 墙钟不按 trial 数;`speedup_vs_eager` 不可报;
形如 `*_DECIDED` 的过去分词事件只表示该步跑完,判决在 payload 里。
**未核实的一律标"未核验",不生成看似正确的新数字**——这是你自己提的要求,我照做了。

---

## 第一部分:你说对了什么

### 1.1 P2′ 三项指控 —— 全部成立,已修复

**接受,无保留。** 审计 `docs/audit-p2prime-reader-defects.md`;修复 commit `bd65af0`。

| 你的指控 | 核实结果 |
|---|---|
| §2 符号/分母不一致 | **成立**。`scanner.py:421` 是 `1−N/F`,reader 曾返回 `(n−f)/n`。**12/12 contrast 符号相反**。你给的 F=10/N=8 反例算术正确,已写成可执行断言 |
| §3 跨候选混池 | **成立**。晚期 contrast **90–98% 样本来自其他候选**(nAll 516 vs nCand 41) |
| §4 留出泄漏 | **成立且结构性**。`orchestrator.py:1324` 先写 `TRIAL_DONE`,`:1335` 才写 `SCAN_BLOCK_DONE` ⇒ **12/12 与 11/11 全部泄漏自己的 4 个点** |

**你 §3 关于作用域的警告我照做了,没有猜。** 你写"不要随意补成'仅当前 space'或
'候选所有 space'就宣称复现旧 analyst"。核实路径:`crun.trials` 在 `orchestrator.py:1333`
累积、`:2461` 跨空间扩展时**从不清空**,并在 `:1033`/`:1504`/`:1959` 原样传给
`stats_analyzer.analyze` ⇒ **legacy 基线的真实作用域是「该候选、其全部空间」**。
缩到单空间会**低估** legacy 基线,所以我用 per-candidate。

**修法**:四种定义并排输出(`old` / `signfix` / `scoped` / `noleak`),各带自己的 n 与 pool 大小;
`--definition old` 可再生历史表(你要求的"保留旧定义与旧数字");
拒答按你的要求分四类计(`no_slope` / `values_unsampled` / `thin_bucket` /
`endpoints_not_journalled`),并明确印成**分母缺失**而非条件化胜出。

### 1.2 §6 关于 N1 —— **这条推翻了我三处结论,是本轮最重要的反馈**

你写"四个 source SHA 相同只固定种子源码,不固定参数、搜索轨迹、实际实现路径"。
我去测了两臂各种子的**最优配置是否逐位相同**:

| 种子 | B−A | 配置相同? | 差异 |
|---|---|---|---|
| 1ced7dd0 | **+10.85%** | **否** | **差 11 个 knob**,含 `COMPUTE_DTYPE: bf16 vs tf32`、`GEMM_BLOCK_N: 64 vs 256` |
| 20eb42e3 | +4.97% | 否 | 差 5 个,含 `COMPUTE_DTYPE: fp16 vs bf16` |
| 49d10c6b | −0.51% | 否 | 差 5 个 |
| 6012d873 | −1.41% | 否 | 差 3 个 |

⇒ **10.85% 不是测量噪声,是两条搜索轨迹各自最优之差。** 你的措辞
("不能称总体噪声分布")完全正确。

于是我做了你隐含要求的那个测量——取两臂**都测过且 knob 值逐位相同**的配置:

```
共有 byte-identical 配置:13 个
|B−A|:  min 0.01%   median 1.03%   p90 2.50%   max 5.33%
```

**我因此撤回三处自己的结论:**

1. **"配对噪声底 10.85%"作废** —— 名字就是错的。
2. **"τ=4% 比真实噪声小 2.7 倍"作废** —— 实测同配置噪声 median 1.03% / p90 2.50% /
   max 5.33% ⇒ **4% 落在 p90 与 max 之间,保守但不荒谬**。
   你 §6 "拒绝'4% 普遍无效'不等于认证'4% 已有效'"——接受。
   **v5 的正确要求是「每个目标 × 每个场地都要有自己的同配置 A/A 标定,4% 不能默认外推」,
   不是「4% 从来不合法」。请按这个措辞改,不要沿用我上一轮的说法。**
3. **"六个投递点全部不可判"作废** —— 我上一轮为修正 4% 自相矛盾而改用 10.85% 判 tie,
   **那是把另一把错尺子换上去**。投递点比的是同一空间内同一配置族的不同取值,
   更接近同配置比较 ⇒ tie 带用 **5.33%**(同配置 p100)。

**重判结果**(败幅 `(at−else)/else`):

| τ | 判决 |
|---|---|
| 4%(预注册下限) | 0 赢 / **4 输** / 2 平 |
| **5.33%(同配置 p100)** | 0 赢 / **4 输** / 2 平 ← 采用 |
| 10.85%(搜索轨迹差,**误用**) | 0 赢 / 0 输 / 6 平 |

**4% 与 5.33% 给出相同判决** ⇒ 结论对阈值选择不敏感。
败幅 8.76 / 8.29 / 6.93 / 5.41 超带,2.21 / 2.33 在带内。

**P3 仍降级,但理由必须改写。** 预注册规则是"A/A 差 >2% ⇒ P3 降为描述性":
按**同配置**噪声(1.03%)**不触发**,按**搜索轨迹差**(11.72%)才触发。仍保守降级,
但理由从"测量噪声大"改为**"搜索路径分歧本身就是 12h 单 run 的固有方差"**——
M1 比较的正是两条轨迹的终局。这与你 §6"搜索结果差异不等于端点测量噪声分布"一致。

### 1.3 §6 的另一半指控 —— **在本批日志上不成立**

你写"N1 reader 用每个 candidate 最后出现的非空 `TUNING_DONE.best_ms` 覆盖前值,
最后一次空间调参结果不保证是候选历史全局最优"。

**实测 8/8 候选 LAST == MIN**,且都等于该候选全部 complete trial 的最小值:

| 种子 | n1-a 的 TUNING_DONE 序列 | n1-b 的序列 |
|---|---|---|
| 6012d873 | [3.5533] | [3.5031] |
| 20eb42e3 | [3.9337] | [4.4810, **4.1293**] |
| 49d10c6b | [7.0635, 7.0635] | [7.1199, **7.0278**] |
| 1ced7dd0 | [5.4175] | [6.1911, **6.0052**] |

⇒ 这批日志里每次扩展后的重调恰好都改善了,所以 `last` 没有覆盖更好的值。
**你的指控在逻辑上正确**(若某次扩展变差就会覆盖),已记为待修的读数器脆弱点,
**但现有数字不受影响**。请不要在修订版里把它写成"已发生的错误"。

### 1.4 §5 / §9 / §10 / §11 —— 接受

| 你的条目 | 我的处理 |
|---|---|
| **§5** H2 应拆三层(H2a 预测可靠性 / H2b sampler 行动效用 / H2c rewriter 结构收益) | **接受**。我原先的两分法把 H2b 与 H2c 混了;窗口 1 只测了 **H2b** |
| **§5** 终局 reader 混合 partners、两侧机会数不同(n=5 vs 25)、"双侧 n≥2"是桶内 trial 计数而非同一完整配置的两次 fresh 重复 | **接受,全部实测确认**。措辞改为**"既定终局按值指标未见赢家;净收益尚未建立"**,不写"已否证" |
| **§9** `off/observe/active` 不是 baseline/C1/C1+C2 | **接受,我的映射是错的**。`v3.diagnosis.mode`(`config.py:412-423`)独立于 `v4.conditional_scan.mode`;窗口 1 的 off 臂配置里 `diagnosis.mode: vector` ⇒ **off 臂并非"无 C1"**;`observe` 不准入 C4 也不交付简报(`orchestrator.py:1437-1442`)⇒ 不是"只有 C1"臂 |
| **§9** "二因子仍可能成立,不是理论上不可识别" | **接受,我上一轮表述过强**。改为:**"当前实现下不可直接组合(C4 轴域来自探针发现的墙,`mode=off` 同时关掉取得与解释);要做二因子需先拆开轴域取得与解释,这是一项工作包,不是配置组合。"** |
| **§10** `profile` 是 optional;`TRIAL_DONE` 含失败与 reused | **接受,覆盖表已交**,见第三部分 |
| **§10** "四个 run × 3,000" 的算术 | **接受,是我笔误**。正确是 720+840+760+680 = **3,000 合计** |
| **§11** 拒绝率下降是代理,不是"同一物理墙被解除" | **接受**。措辞改为"采样拒绝率代理下降"。你指出的三条选择条件我确认存在(只纳入有 complete best 的 rewrite、只取第一个 parent、`briefed` 是全程集合而非时序前置),**已随分母披露** |

---

## 第二部分:你的两处推论需按实测更正

### 2.1 §2 "当前证据不支持预判更正方向" —— 实测给出了方向

| run | 定义 | n | 边际中位误差 | 条件化 | 倍数 | 逐例胜 |
|---|---|---|---|---|---|---|
| l3:43 | old(已作废) | 12 | 0.1963 | 0.0039 | 50.9x | 11/12 |
| | signfix | 12 | 0.1795 | 0.0039 | 46.5x | 12/12 |
| | scoped | 12 | 0.2392 | 0.0039 | 62.0x | 10/12 |
| | **noleak(最严)** | **7** | 0.1390 | 0.0069 | **20.3x** | **7/7** |
| l3:21 | old(已作废) | 11 | 0.0802 | 0.0031 | 25.8x | 10/11 |
| | signfix | 11 | 0.0944 | 0.0031 | 30.3x | 11/11 |
| | scoped | 11 | 0.2471 | 0.0031 | 79.3x | 11/11 |
| | **noleak(最严)** | **9** | 0.1766 | 0.0017 | **104.8x** | **9/9** |

⇒ **8 组读数全部条件化胜出;修全部三项缺陷后逐例胜率反而升到 7/7 与 9/9**
(带缺陷的旧定义反而是 11/12、10/11)。**修正让结论更干净,不是更弱。**

**所以请这样写**(而不是"优势待定"):

> 条件化优于边际在四种定义下均成立(8 组读数,倍数 20x–105x,最严定义下逐例胜 7/7 与 9/9);
> **先前发表的 50.9x / 25.8x 两个点估计不可引用**,它们同时含三项缺陷。
> 任何倍数必须与它的定义和 n 一起给。

**同时接受你的三条限定**:`n` 变小本身是发现(剔除泄漏后 12→7、11→9 ⇒
**5 个与 2 个 contrast 的边际预测本来完全建立在自己的留出点上**,按分母缺失报告);
**23 个 contrast 来自 2 个 run,不是 23 次独立搜索重复**;
"更早"需独立的时点或成本证据,预测精度不蕴含它。

### 2.2 §12 "三张卡与四个臂槽自相矛盾" —— 你误读了,但我的原文确实有歧义

事实:**两台机器,每台 2 张 4090,共 4 张**。"三张可用卡"是 box1 升双卡**之前**的过时记录。
当前实测:box4 GPU0/GPU1 + box1 GPU0/GPU1 全部在用,窗口 2 四个 run 各占一张,
GPU 分离已在运行期查证并存盘(两台都印 `SEPARATED`,首次尝试即答)。

⇒ **"一个窗口 = 4 个臂"是对的。** 但**你的担忧成立**,是我原文没写清:
**同机配对约束下,一台机器只能承载一个完整的对(2 臂)** ⇒
**嵌套三臂装不进一台机器**,必须分两批跑,或接受跨机分层报告(不可并表)。
请在修订版里保留这个约束,不要写成"一个窗口可完成三臂研究"。

---

## 第三部分:你要的五项交付物

| # | 你的要求 | 状态 |
|---|---|---|
| 1 | 逐点答复,带 source/日志/版本证据 | ✅ `docs/reply-to-feedback-review-research-v5.md` |
| 2 | P2′ 逐 contrast 表(g_d、y、g_m、信息截止、作用域、分母) | ✅ 修好的 reader 按四定义并排输出,含 pool 大小、拒答原因、泄漏剔除计数 |
| 3 | 保留旧表与旧定义,另交更正分析与新旧差异 | ✅ `--definition old` 可再生;差异见审计文档 §4 |
| 4 | profile 覆盖表与离线资格说明 | ✅ 见下 |
| 5 | 证据充分后才提 v5 修订 | ✅ 接受,v5 修订暂缓至 M1/M2b 出结果 |

**交付物 4 的读数**(`scripts/probes/v41_profile_coverage.py`):

| 项 | pilot-p1 | pilot-p2 |
|---|---|---|
| `TRIAL_DONE` | 760 | 680 |
| complete / **fail** | 612 / **148** | 571 / **109** |
| **reused(非独立测量)** | **43** | **41** |
| 无 profile 对象 | 148(**= 全部 fail**) | 109(= 全部 fail) |
| `peak_alloc_bytes` 可用 | 612(**80.5%**) | 571(**84.0%**) |
| `wall_ms` / `cpu_issue_ms` / `overhead_gpu_ms` | **present 但 0% 非空** | 同 |
| 精度分布 | bf16 437 / fp16 159 / tf32 88 / ieee 76 | fp16 397 / tf32 121 / bf16 84 / ieee 78 |

**三条离线资格结论(请写进修订版):**
1. **可重评分子集是 80.5% / 84.0%,不是 100%。** 失败记录**全部无 profile** ⇒
   跨目标冠军必须**在该子集内**读并报告被排除数,否则就是按可得性选择。
2. **`wall_ms` / `cpu_issue_ms` / `overhead_gpu_ms` 三个字段 present 但恒 null** ⇒
   任何依赖它们的目标(如含 CPU 开销的端到端时间)**现在不可离线重评分**。
3. **精度混杂是决定性障碍**:两个 run 的 trial 横跨 4 种精度。你 §10 说
   "不同精度血缘不能未经限定就归为'只换了目标'"——成立 ⇒
   **内存目标的离线重评分必须按精度分层**,否则"换目标改变了冠军"可能只是
   "换精度改变了冠军"。

**接受你关于 `peak_alloc_bytes` 语义的警告**(`worker_main.py:412-450`、`:498-540`):
它是**进程 allocator 峰值**,helper 内另构造一份模型时外层 metadata 模型仍存活 ⇒
**不是候选独占显存**;`peak_above_resident_bytes` 扣除 resident 后仍含候选权重与输入。
⇒ 只能作为**明确声明范围的 proxy**,标注必须随数字给。
⚠ **这一条我未独立复核 worker 代码路径,按你的锚点采信,标为"待独立复核"。**

---

## 第四部分:明确标为"未核验"的四项

| 项 | 为什么未核验 |
|---|---|
| `peak_alloc_bytes` 的 allocator 语义与 `.to(device)` 精度匹配 | 按你的锚点采信,我未独立读该路径 |
| 更早 run 与历史 reader 版本的对应 | 本次只核实窗口 1 的四个 run |
| N1 reader 的 `last` 覆盖在**其他** run 上是否发生 | 只测了 n1 对(8/8 未发生) |
| v5 §13 各面板的实际 GPU 成本 | 需 pilot,未做 |

**请不要为这四项生成数字。** 若修订版需要它们,请列为"待测量"。

---

## 第五部分:请你在修订版里做的事

现在证据够了,可以动 v5 文本。**任务仍是修订,不是重写。**

1. **按 1.2 改 N1 与阈值的全部表述。** 区分"同配置测量噪声(median 1.03% / p90 2.50% /
   max 5.33%)"与"搜索轨迹差(11.72%)";τ=4% 的问题不是"不合法",而是"不能默认外推";
   P3 降级保留但理由改为搜索路径方差。**我上一轮 prompt 里关于 4% 的说法请整体作废。**
2. **按 1.4 把 H2 拆成 H2a/H2b/H2c**,并标明窗口 1 只测了 H2b。
3. **按 1.4 改 §12.1 的处理矩阵定义**:先定义 C1 指"解释"还是"墙与轴的取得",
   再列共有测量、轴域、提示内容与 C2 消费;把"当前实现下不可直接组合"写成工作包,
   不写"理论不可识别"。
4. **按第三部分把离线重评分的资格条件写进 §12.2 与 G 面板**:
   子集比例、恒 null 字段、**必须按精度分层**。
5. **按 2.2 保留同机配对约束**:一台机器一个完整的对;三臂需分两批或跨机分层。
6. **保留你原有的保守措辞与"明确非目标"章节**;不新增平台、证据账本、监控、审批队列;
   不恢复已撤销的独立 C3;不追溯改写 legacy 的预注册含义。
7. **若你认为本 prompt 的某条与证据冲突,指出冲突并给你的依据**,不要沉默照做。
   上一轮你正是这样纠正了我三处,那是有价值的。

**交付**:修订后的 v5 全文;每处依据实测的改动在正文标出数字与来源(哪个 run、n 多少);
不写"实验显示"而不给数字。

**关于在跑实验**:窗口 2 的四个 run(M1 主对照对 + M2b 复制对)正在跑,
**不因本轮反馈重启**——三项 P2′ 缺陷全在读数器,
`git diff --name-only <启动 commit>..HEAD | grep -E "^(src/|configs/)"` 返回 **NONE**。
两台机器已在 `bd65af0`,全测试 **1252 passed / 1 skipped**(含 11 个新正对照)。
**不要提出任何需要改动在跑版本的建议。**

=== PROMPT 结束 ===
