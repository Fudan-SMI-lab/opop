# 对 `feedback-review-research-v5.md` 的逐点答复

> 日期:2026-09-15。回应对象:外部 agent 的独立反馈(§0–§13)。
> **本文的规矩**:每条只答"接受 / 部分接受 / 争议",并给**实测数字或 `file:line`**。
> 凡未核实的一律标"未核验",不生成看似正确的新数字(这是反馈末尾的明确要求)。
> 本文不修改 v5 草案,也不动在跑实验的任何处理。
>
> **摘要:反馈的技术指控我核实了 6 条,5 条成立(其中 2 条我已修复,1 条推翻了我自己的
> 结论),1 条在本批日志上不成立。反馈本身有 2 处推论超出其证据,已给出实测反证。**

---

## 0. 交付物完成情况(对应反馈 §13)

| # | 反馈要求 | 状态 |
|---|---|---|
| 1 | 逐点答复,带 source/日志/版本证据 | **本文** |
| 2 | P2′ 逐 contrast 表:g_d、y、g_m、信息截止、作用域、分母 | **已交**:`v41_p2prime.py` 现按四定义并排输出,含 pool 大小与拒答原因 |
| 3 | 保留旧 P2′ 表与旧定义,另交更正分析与新旧差异 | **已交**:`--definition old` 可再生历史表;差异见 `audit-p2prime-reader-defects.md` §4 |
| 4 | profile 覆盖表与离线资格说明 | **已交**:`scripts/probes/v41_profile_coverage.py`,读数见本文 §5 |
| 5 | 证据充分后才提 v5 修订 | **接受**:v5 修订暂缓,先补证据 |

---

## 1. P2′ 三项指控 —— **全部成立,已修复**

**接受,无保留。** 完整审计:`docs/audit-p2prime-reader-defects.md`;
修复:commit `bd65af0`;正对照:`tests/test_v41_p2prime_reader.py`(11 个)。

| 指控 | 核实结果 |
|---|---|
| §2 符号/分母 | **成立**。`scanner.py:421` 是 `1−N/F`,reader 曾返回 `(n−f)/n`。**12/12 contrast 符号相反**。你给的 F=10/N=8 反例算术正确,已写成可执行断言 |
| §3 跨候选混池 | **成立**。晚期 contrast **90–98% 样本来自其他候选**(nAll 516 vs nCand 41) |
| §4 留出泄漏 | **成立且结构性**。`orchestrator.py:1324` 先写 `TRIAL_DONE`,`:1335` 才写 `SCAN_BLOCK_DONE` ⇒ **12/12 与 11/11 全部泄漏自己的 4 个点** |

**§3 关于作用域的警告我照做了,没有猜。** 你写"不要随意补成'仅当前 space'或'候选所有 space'
就宣称复现旧 analyst"。核实结论:**legacy 基线的真实作用域是「该候选、其全部空间」**——
`crun.trials` 在 `orchestrator.py:1333` 累积、`:2461` 跨空间扩展时**从不清空**,
并在 `:1033` / `:1504` / `:1959` 原样传给 `stats_analyzer.analyze`。
缩到单空间会**低估** legacy 基线,所以我用了 per-candidate 作用域。

**一处争议:你 §2 说"当前证据不支持预判更正方向"。实测给出了方向。**

| run | 定义 | n | 边际中位误差 | 条件化 | 倍数 | 逐例胜 |
|---|---|---|---|---|---|---|
| l3:43 | old(已作废) | 12 | 0.1963 | 0.0039 | 50.9x | 11/12 |
| | **noleak(最严)** | **7** | 0.1390 | 0.0069 | **20.3x** | **7/7** |
| l3:21 | old(已作废) | 11 | 0.0802 | 0.0031 | 25.8x | 10/11 |
| | **noleak(最严)** | **9** | 0.1766 | 0.0017 | **104.8x** | **9/9** |

⇒ 四定义 × 两 run = 8 组读数**全部条件化胜出**;修全部三项缺陷后**逐例胜率反而升到 7/7 与 9/9**。
**所以:两个点估计(50.9x / 25.8x)作废,方向存活。** 建议措辞改为:

> 条件化优于边际在四种定义下均成立(8 组读数,倍数 20x–105x,逐例胜 7/7 与 9/9);
> **先前发表的 50.9x / 25.8x 不可引用**,它们含三项缺陷。

**接受你的其余方法学要求**:拒答四类分开计(`no_slope` / `values_unsampled` /
`thin_bucket` / `endpoints_not_journalled`,现已逐类打印);主分母含 unresolved;
**23 个 contrast 来自 2 个 run,不是 23 次独立搜索重复**(已写进 pooled 输出的注脚);
"更早"需独立的时点或成本证据,预测精度不蕴含它。

---

## 2. §6 关于 N1 —— **两条指控分开:一条不成立,一条成立且推翻了我的结论**

### 2.1 "reader 用最后一次 TUNING_DONE 覆盖前值,可能不是全局最优" —— **本批日志上不成立**

**实测**(逐候选列出所有 `TUNING_DONE.best_ms`):

| 种子 | n1-a 序列 | n1-b 序列 | LAST == MIN? |
|---|---|---|---|
| 6012d873 | [3.5533] | [3.5031] | ✔ |
| 20eb42e3 | [3.9337] | [4.4810, **4.1293**] | ✔ |
| 49d10c6b | [7.0635, 7.0635] | [7.1199, **7.0278**] | ✔ |
| 1ced7dd0 | [5.4175] | [6.1911, **6.0052**] | ✔ |

**8/8 候选 LAST 都等于 MIN**,且都等于该候选全部 complete trial 的最小值。
⇒ 这个缺陷**在这批日志上没有发生**(扩展后的重调恰好都改善了)。
**但你的指控在逻辑上正确**:若某次扩展变差,`last` 会覆盖更好的值。已记为待修的读数器脆弱点,
不影响现有数字。

### 2.2 "四个 source SHA 相同只固定种子源码,不固定参数/搜索轨迹" —— **成立,而且后果比你写的更大**

**这条我必须完整承认:10.85% 不是"同一固定配置的重复测量"。** 实测两臂各种子的最优配置:

| 种子 | B−A | 配置逐位相同? | 差异 |
|---|---|---|---|
| 1ced7dd0 | **+10.85%** | **否** | **差 11 个 knob**,含 `COMPUTE_DTYPE: bf16 vs tf32`、`GEMM_BLOCK_N: 64 vs 256` |
| 20eb42e3 | +4.97% | 否 | 差 5 个,含 `COMPUTE_DTYPE: fp16 vs bf16` |
| 49d10c6b | −0.51% | 否 | 差 5 个 |
| 6012d873 | −1.41% | 否 | 差 3 个 |

⇒ **10.85% 是"两条不同搜索轨迹各自找到的最优之差",不是测量噪声。**
我先前把它当"配对噪声底"是**错的**,你 §6 的措辞("不能称总体噪声分布")是对的。

**于是我做了你隐含要求的那个测量**:取**两臂都测过的、knob 值逐位相同**的配置,
比较其延迟——这才是真正的同配置 A/A。

```
两臂共有的 byte-identical 配置:13 个
TRUE same-config A/A |B−A|:  n=13  min 0.01%  median 1.03%  p90 2.50%  max 5.33%
```

**⇒ 真正的测量噪声底是 median 1.03% / max 5.33%,不是 10.85%。**

**三条直接后果,全部对我不利:**

1. **P3 的降级依据被推翻。** 预注册规则是"若 A/A 差 >2% ⇒ P3 降为描述性"。
   按**同配置**噪声(median 1.03%)该规则**不触发**;按**搜索轨迹差**(11.72%)才触发。
   ⇒ **必须区分两个量,并明确 P3 的分母是哪一个。** 我倾向仍按 11.72% 保守处理
   (M1 比较的正是"两条搜索轨迹的终局"),但**理由要改**:不是"测量噪声大",
   而是"**搜索路径分歧本身就是 12h 单 run 的固有方差**"。这与你 §6
   "搜索结果差异不等于端点测量噪声分布"完全一致。
2. **我用 10.85% 判六个投递点全平,这把尺子用错了。** 投递点比较的是**同一空间内
   同一配置族的不同取值**,更接近同配置比较 ⇒ 该用 **5.33%(同配置 p100)**,不是 10.85%。
   **重判:六个点中 8.8% / 8.3% / 6.9% / 5.4% 四个超过 5.33% ⇒ 回到"4 输 2 平"。**
   我上一轮说"全部不可判"**过度收缩了**,现予更正。
3. **`gates.py:34` 的 τ=4% 下限,按同配置噪声(median 1.03%、p90 2.50%)是合理的**,
   不是我先前说的"比真实噪声小 2.7 倍"。**我先前那句话作废。**
   你 §6 "拒绝'4% 普遍无效'不等于认证'4% 已有效'"——**接受**,并且实测显示
   4% 在**这个任务、这个精度、这台机器上**位于 p90(2.50%)与 max(5.33%)之间,
   是保守但不荒谬的选择。**v5 的正确要求不是"4% 从来不合法",而是
   "每个目标 × 每个场地都要有自己的同配置 A/A 标定,4% 不能默认外推"。**

**我为此撤回三处表述**:①"配对噪声底 10.85%";②"4% 比真实噪声小 2.7 倍";
③"六个投递点全部不可判"。

---

## 3. §5 关于 H2 拆分与行动层 —— **接受拆分,接受"尚未建立"的措辞**

**接受**:H2 拆成 **H2a**(响应预测可靠性)/ **H2b**(sampler 行动效用)/
**H2c**(rewriter 结构收益)。我原先的两分法把 H2b 与 H2c 混在一起,
而窗口 1 只测了 H2b。

**接受**你 §5 关于终局 reader 的三条限定,全部实测确认:
- 它**混合 partners**(按轴值分桶,桶内 partner 不固定);
- **两侧机会数不同**(n=5 vs 25、n=2 vs 25),极小值受搜索机会数影响;
- **"双侧 n≥2"是桶内 trial 计数,不是同一完整配置的两次 fresh 重复**。

⇒ 措辞按你的建议改:**"六次 E 的既定终局按值指标未见赢家;净收益尚未建立"**,
不写"已否证"。**同时按 §2.2 的更正,败幅判据用 5.33% 而非 10.85% ⇒ 4 输 2 平。**

**你 §5 指出我 4% 自相矛盾 —— 成立,已改。** 但改法与你我原先设想的都不同:
不是"因为 4% 不合法所以全平",而是**"tie 带必须来自同配置 A/A 标定"**,
而那个数是 5.33%(p100)。这样 4% 与 5.33% 就不冲突了,前者是全局预注册下限,
后者是本场地实测上限。

---

## 4. §9 关于开关语义 —— **接受,我的映射是错的**

**接受**。我先前说 `off/observe/active` 就是 baseline/C1/C1+C2 三臂,**这是错的**:

- `v3.diagnosis.mode`(`config.py:412-423`)决定**资源向量是否进 prompt**,
  与 `v4.conditional_scan.mode` **独立**;`mode=off` 的 run 仍可有 C1 向量进 prompt。
  实测印证:窗口 1 的 off 臂配置里 `diagnosis.mode: vector` —— **off 臂并非"无 C1"**。
- `observe` **不准入 C4、不交付条件简报**(`orchestrator.py:1437-1442`)⇒
  它不是"只有 C1"臂,而是"探针跑了但什么都不消费"。

⇒ **`off/observe/active` 不构成 baseline/C1/C1+C2。** 你 §9 的要求正确:
**先定义 C1 指"解释"还是"墙与轴的取得",再列处理矩阵。**

**部分争议**:你说"二因子仍可能成立,不是理论上不可识别"。**同意**,
但要补一句实现事实:**C4 的扫描轴域来自探针发现的墙**,所以要做真二因子,
必须让"C1 off"臂仍能提供轴域(即拆开"取得"与"解释")。
这是**可实现的**,但**是一项新工作**,不是配置组合。我先前说"理论不可识别"**表述过强**,改为:
**"当前实现下不可直接组合;要做二因子需先拆开轴域取得与解释,这是一项工作包。"**

---

## 5. §10 关于离线资产 —— **接受,覆盖表已交**

**接受两处事实更正**:
- 我写"四个 run × 3,000 trial"是**笔误**,正确是 **720+840+760+680 = 3,000 合计**。已改。
- `profile` 是 optional,`TRIAL_DONE` 含失败与 reused 记录 —— **成立**。

**覆盖表实测**(`scripts/probes/v41_profile_coverage.py`):

| 项 | pilot-p1 | pilot-p2 |
|---|---|---|
| `TRIAL_DONE` | 760 | 680 |
| complete / fail | 612 / **148** | 571 / **109** |
| **reused(非独立测量)** | **43** | **41** |
| 无 profile 对象 | 148(= 全部 fail) | 109(= 全部 fail) |
| `peak_alloc_bytes` 非空 | 612(**80.5%**) | 571(**84.0%**) |
| `wall_ms` / `cpu_issue_ms` / `overhead_gpu_ms` | **present 但 0% 非空** | 同 |
| 精度分布 | bf16 437 / fp16 159 / tf32 88 / ieee 76 | fp16 397 / tf32 121 / bf16 84 / ieee 78 |

**三条离线资格结论:**
1. **可重评分子集是 80.5% / 84.0%**,不是 100%。失败记录**全部无 profile** ⇒
   任何跨目标冠军必须**在该子集内**读,并报告被排除的记录数,否则就是按可得性选择。
2. **`wall_ms` / `cpu_issue_ms` / `overhead_gpu_ms` 三个字段 present 但恒 null** ⇒
   任何依赖它们的目标(如"含 CPU 开销的端到端时间")**现在不可离线重评分**。
3. **精度混杂是决定性障碍**:两个 run 的 trial 横跨 4 种精度。
   你 §10 说"不同精度血缘不能未经限定就归为'只换了目标'"——**成立**。
   ⇒ 内存目标的离线重评分**必须按精度分层**,否则"换目标改变了冠军"可能只是
   "换精度改变了冠军"。

**接受**你关于 `peak_alloc_bytes` 语义的警告(`worker_main.py:412-450`、`:498-540`):
它是**进程 allocator 峰值**,helper 内另构造一份模型时外层 metadata 模型仍存活 ⇒
**不是候选独占显存**;`peak_above_resident_bytes` 扣除 resident 后仍含候选权重与输入。
⇒ **这些量只能作为明确声明范围的 proxy**,标注必须随数字一起给。**这一条我未独立复核
worker 代码路径,按你的锚点采信,标为"待我独立复核"。**

---

## 6. §11 关于拒绝率代理 —— **接受降级为代理**

**接受**。措辞改为**"采样拒绝率代理下降"**,不写"同一物理墙已被解除"。
你指出的三条选择条件我确认存在于 `v41_refusal_delta.py`:
只纳入有 complete best 的 rewrite、只取第一个 parent、`briefed` 是全程集合而非时序前置。
⇒ 这三条**已随分母披露**在该脚本输出里,但**文档表格里没写**,已补。

**保留**:P1 个案(24%→8%、BN=256 通过率 1/6→9/11、best −17.0%)作为**提示性对照**;
**off 臂也 4/8 解开** 与**两个 active pilot 方向相反**(3/4 vs 0/4)必须同时报。
**不把 3/4 当方法因果胜率** —— 接受。

---

## 7. 两处反馈自身的问题(附实测反证)

**① §2 "当前证据不支持预判更正方向"** —— 见本文 §1:8 组读数全部条件化胜出,
最严定义下逐例胜 7/7 与 9/9。**方向是可预判的,已实测。**

**② §12 "外部材料同时写'三张可用卡'与'四个并行臂槽',可用容量存在内部不一致"**
—— **这条是你误读,但我的原文确实有歧义。** 事实:**两台机器,每台 2 张 4090,共 4 张**。
"三张可用卡"是一条更早的历史记录(box1 升级双卡**之前**),已过时。
当前实测(2026-09-15):box4 GPU0/GPU1 + box1 GPU0/GPU1 = **4 张,全部在用**
(窗口 2 的四个 run 各占一张,GPU 分离已运行期查证并存盘)。
⇒ **一个窗口 = 4 个臂是正确的**,但**同机配对约束**下,一台机器只能承载**一个完整的对**
(2 臂),所以"4 臂"= 2 对 = 2 台机器。**三臂设计会跨机 ⇒ 不可并表** —— 你 §12 这个
担忧**成立**,是我原文没写清:**嵌套三臂必须挤在一台机器上分两批跑,或接受跨机分层报告。**

---

## 8. 未核验项(明确标注,不生成数字)

| 项 | 为什么未核验 |
|---|---|
| `peak_alloc_bytes` 的 allocator 语义与 `.to(device)` 精度匹配 | 按你的 `worker_main.py` 锚点采信,我未独立读该路径 |
| 历史 reader 版本与更早 run 的对应 | 窗口 1 之前的 run 未重读;本次只核实窗口 1 |
| N1 reader 的 `last` 覆盖在**其他** run 上是否发生 | 只测了 n1 对(8/8 未发生) |
| `v5` 草案 §13 各面板的实际 GPU 成本 | 需 pilot,未做 |

---

## 9. 对在跑实验的影响:零

窗口 2 四个 run(M1 对 + M2b 对)**继续,不重启**:
- 三项 P2′ 缺陷全在**读数器**;`git diff --name-only bb15839..HEAD | grep -E "^(src/|configs/)"`
  返回 **NONE** ⇒ orchestrator 与 worker 路径**未被触碰**;
- 两台机器已在 `bd65af0`,**1252 passed / 1 skipped**(+11 正对照);
- 四个 run 的种子导入 `rejected: []`,**M1 两臂四个 `source_sha` 逐位相同**。

---

## 10. 下一步(按你 §12 的优先级表)

| 优先级 | 我要做的 | 完成标准 |
|---|---|---|
| P0 | ✅ 已完成:P2′ 修核 + 11 个已知答案 fixture(反向验证 6/11 会红) | 同公式、per-candidate 池、按 trial_id 剔除泄漏 |
| P1 | **修正 N1 表述**:区分"同配置测量噪声 1.03%/5.33%"与"搜索轨迹差 11.72%";H2 拆三层 | 两个量不混称,P3 分母明确 |
| P2 | ✅ 覆盖表已交;**离线重评分待做,必须按精度分层** | 缺失与边界明确,共同有效交集可读 |
| P3 | v5 局部文字更新 —— **等 M1/M2b 结果** | 保留 legacy,无新平台 |
