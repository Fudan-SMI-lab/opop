# v3 缺口清单与修复情况

**本文档是缺口的唯一权威清单**,记录每条缺口的现状、修复状态与判据。任何"已修复"的断言都带 commit 与实测证据。

**最后更新**:2026-09-10(第三轮:G18–G21 全部关闭,G9 实验进行中,G12 已解锁待落地) · **v3 commit**:见每条的证据列

**机器**:box 1 / box 2 = RTX 4090(sm_89) · **box 3 = A800 80GB PCIe(sm_80)**,配置记录见 `docs/box-a800-setup.md`

---

## 零、清单的分类原则

| 类型 | 含义 | 处理方式 |
|---|---|---|
| **算错了** | 有这个量,但算法产出的数不是它声称的东西 | **必须修**,优先级最高 |
| **方向错了** | 目标函数或判据的层级关系不对 | **必须修**,优先级最高 |
| **缺失** | 这个量根本不存在 | 按成本与收益排 |
| **测不到** | 知道该测什么,但没有可行手段 | 记录 + 找替代下界/上界 |
| **未验证** | 知道怎么做,但没有证据说明有效 | 只能靠实验,不能靠设计 |

---

## 一、清单主表

| # | 缺口 | 类型 | 严重度 | 状态 | 证据 |
|---|---|---|---|---|---|
| **G1** | 搬运开销的分子是任务级常数 ⇒ 该"维度"退化成 1/延迟 | 算错了 | 高 | **✅ 已修 `9e538b9`** | §G1 |
| **G2** | 算力开销的分子同上 | 算错了 | 高 | **✅ 已修 `9e538b9`**(候选级 FLOP 证明不可测,已记录原因) | §G2 |
| **G3** | "资源→性能"没有强制兑现 ⇒ 资源改善但耗时不动的改动会被当成成功 | 方向错了 | 高 | **✅ 已修**(新增 `evaluation/conversion.py`,双向反验证) | §G3 |
| **G4** | 显存峰值维度整个缺失 | 缺失 | 中 | **✅ 已修 `9e538b9`**(端到端实测) | §G4 |
| **G5** | 片上容量(寄存器/共享内存)只当布尔判据,没当可交换的软资源 | 缺失 | 中 | **✅ 已修**(余量作为量报告给每个判决) | §G5 |
| **G6** | 候选自己的真实 DRAM 流量测不到 | 测不到 | 中 | **🟡 已修 `9e538b9`**(下界+上界已落地;精确值仍不可测) | §G6 |
| **G7** | L2 命中被算成 DRAM 流量 ⇒ 纯 L2 kernel 读出屋顶 287% | 算错了 | 中(今天休眠) | **✅ 已修 `9e538b9`**(必败测试通过) | §G7 |
| **G8** | 禁止跨维度比"谁变化最大"(共享内存中位摆幅是寄存器的 13.5 倍) | 方向错了 | 中 | **✅ 已登记为纪律** | §G8 |
| **G9** | 给 AI 更多信息不保证更好(外部实测 10%→6%) | 未验证 | 高 | **🟡 首份结果已出**(3 臂最好成绩落在 **1.84%** 内 ⇒ 噪声底以内,**没有任何一臂分离**;n=4 调用,只是轶事) | §G9 |
| **G10** | 后端天花板:拿 cuBLAS 成绩当物理屋顶,而候选全是 Triton | 算错了 | 中 | **✅ 已修 `a2f32e6`**(实测否证外部 54%,并发现缺陷是**双向**的) | §G10 |
| **G11** | 维度集合的"运行期发现"机制未定 | 缺失 | 低 | **✅ 已修**(主张收窄到真正实现了的部分) | §G11 |
| **G12** | 异构卡按维度比较的办法未定 | 缺失 | 低 | **🟡 已解锁**(A800 到手,已有第一份跨卡实测;各维度量本就是每台机实测,见 G23 裁定) | §G12 |
| **G13** | 撤回 TMA 闭合后没有等价的算错自检 | 缺失 | 中 | **✅ 已修**(新增 `evaluation/reading_checks.py`) | §G13 |
| **G14** | 通信 / 多卡维度不存在 | 缺失 | 低 | ⏳ **范围外**(三台机全是单卡) | §G14 |
| **G15** | 谓词化维度(死分支)不可测 | 测不到 | 中 | ❌ 被计数器权限永久挡住(有替代信号) | §G15 |
| **G16** | **`n_spills` 与 SASS 的 STL/LDL 被当成互相印证,实际是两个不同的量** | 算错了 | 中 | **✅ 已修**(实测 5/5 不符,差 4–33×) | §G16 |
| **G17** | 反汇编器只在 CUDA toolkit 里找,而 **Triton 自带一份** | 缺失 | 低 | **✅ 已修**(box 1 实测存在) | §G17 |
| **G18** | **Linux 移植代码不在分支里**,只以未提交状态存在于 box 1 ⇒ 新克隆的 harness 完全跑不起来 | 缺失 | **高** | **✅ 已修 `44de7e7`**(单一版本双平台;顺带修掉 3 个 Windows 上也存在的 bug) | §G18 |
| **G19** | **给 agent 的散文里写死了硬件比值**(实测数值一直是每台机测的,但**比值**被写成常数) | 算错了 | 中 | **✅ 已修 `44de7e7`**(删 5 处 + 防回归测试) | §G19 |
| **G20** | **每次 rewriter 调用中位 18.9 分钟、33% 撞满超时天花板** ⇒ A/B 样本量被墙钟卡死 | 缺失 | 中 | **🟡 已修 `44de7e7`** 但**修复尚未生效于运行中的实验**:G9 实测 3/4 完成调用仍死在 1500 s | §G20 |
| **G21** | 探针/脚本读事件与元数据时**字段路径全靠猜**,猜错静默读成「零结果」 | 算错了 | 中 | **🟡 已修 `fb0f84f`** 但**当天就复发**(见 G22) | §G21 |
| **G22** | **G21 在「修完之后新写的脚本」里立刻复发** ⇒ 光提供权威读取层不够,不用就没用 | 算错了 | **高** | **✅ 已修 `d66de53`**(A/B 改用 `latency_ms_of` + 防回归测试;实测量全部从磁盘救回,零重跑) | §G22 |
| **G23** | 两台机的 torch/triton 版本不同 ⇒ 跨机无法把差异归因到卡 | ~~算错了~~ **非缺口** | — | **✅ 已关闭(用户裁定)**:工具链与硬件同为「环境」的一部分,**跨机无法归因是预期现象**,不是缺陷。保留为文档事实 | §G23 |
| **G24** | **G20 的产出信号误杀合法调用**:实测 **5/6** 次调用在 12.7 min 被中止,而那 25 个「文件」全是种子输入 | 方向错了 | **高** | **✅ 已修 `待提交`**(判据补上**服务端仍在工作**这一半;7 个防回归测试) | §G24 |
| **G25** | **被上一次 abort 关掉的传输会沉掉下一个调用**:httpx 抛裸 `RuntimeError`,**不是** `AgentCallError` ⇒ 逃出重试循环 | 缺失 | 中 | **✅ 已修 `待提交`**(发请求前自愈重开) | §G25 |
| **G26** | **J2-8 仍未过,且 A800 上更糟**:判据把 L2 内流量读成 DRAM 绑定 | 算错了 | **高(S2 硬门)** | ⏳ **S2 的前置**:修法 (a) 已落地(`_IMPOSSIBLE_FRAC=1.05`),**修法 (b)「工作集 vs L2 容量」未做** | §G26 |



图例:✅ 已完成 · 🔧 修复中 · 🟡 部分解决 · ⏳ 待处理 · ❌ 不可行

---

## 二、逐条

### G1 搬运开销的分子是任务级常数 —— 算错了

**症状**:`bottleneck.py:296-307` 算 `achieved_tbs = byte_count / gpu_ms`,而 `orchestrator.py:1421` 传入的 `byte_count = cost.compulsory_bytes` 是**任务级常数**。约掉之后这个量就是 `常数 / 耗时`。

**磁盘复验(5 个 run,从存盘的 `achieved_tbs × gpu_ms` 反解分子)**:

| run | `gpu_ms` 跨度 | 反解 `byte_count` 跨度 |
|---|---|---|
| L3:21 box1 | 2.8× | 0.183% |
| L3:21 box2 | **4.3×** | 0.359% |
| L3:43 r1 | **4.6×** | 0.185% |
| L3:43 r2 | 1.3× | 0.069% |
| L3:48 | 2.5× | 0.010% |

耗时变 4.6 倍而分子变 0.36%(那 0.36% 只是 4 位小数舍入)。

**根因不是 bug,是用错了地方。** `task_cost.py` docstring 明写这是刻意设计:`compulsory_bytes` 是**去重后的输入+参数+输出**,是任何正确实现的**下界**,而 `flop_count / compulsory_bytes` 回答"这个任务在这张卡上有没有可能算力受限"——**这是任何单候选测量都答不了的问题**。缺陷在于把任务级下界当候选级实测量除以耗时。

**修法(不是替换,是并存 + 分开命名 + 各自只答自己的问题)**:
- 任务级量**保留**,因为它对自己的问题是对的;
- **每份判决的 evidence 里带上一个说明串** `dram_pressure_basis`,原文:*"task-level compulsory bytes / gpu_ms; within a task this is 1/latency rescaled, not a per-candidate traffic measurement"* —— **用文字把它降级**,读到这份证据的人(和 agent)不可能再把它当候选流量;
- **新增候选级量**(见 G4/G6):`candidate_aten_bytes`(下界)、`peak_alloc_bytes`、`threads_launched`,它们回答"这个候选搬了多少"。

**为什么不能直接换成候选级**:那会毁掉唯一能回答"L3:48 有 1.351 GB 固定流量、只剩 10% 空间"的那个量,而这是整个项目最硬的物理结论。

**反向验证**:测试 `test_pressure_fields_declare_that_they_track_latency` 断言 basis 串存在且明说跟随延迟 —— 删掉说明串测试就失败。

**状态**:✅ **已修 `9e538b9`**(box 1 399 passed / 0 failed)。

---

### G2 算力开销的分子同上 —— 算错了

**同一个式子的另一半**:`achieved_tflops = flop_count / gpu_ms`,`flop_count` 也是任务级常数(反解跨度 ≤0.008%)。

**候选级 FLOP 能不能测?实测结论:aten 层测不到。** 用 `FlopCounterMode` 作用在候选上(box 1,L3:21):

| 候选 | FLOP | 相对参考 |
|---|---|---|
| cand-8a5dcef3(冠军,全融合 Triton) | **0** | 0.0000× |
| cand-2d4e1574(全融合 Triton) | **0** | 0.0000× |
| cand-37572704(混合,两个 1×1 交给 cuDNN) | 120,503,484,416 | **1.0748×** |

**读出 0 不是故障,是正确答案**:算术全在 Triton kernel 内部,dispatcher 看不见。而那个混合候选读出 1.07× 参考,因为它把两个卷积交回了 aten。

**所以 `FlopCounterMode` 在候选上测到的是"没融合掉的那部分"** —— 这本身是一个有用的量(它给出融合程度),但**绝不能当作候选的总 FLOP**。

**修法**:同 G1 —— `compute_pressure_basis` 说明串已落地。**候选级 FLOP 字段故意不加**:唯一可行的 aten 路对全融合候选读出 0,而 0 会被下游读成"这个候选不做算术",那是一个比缺失更坏的读数。**缺失是诚实的,可信的 0 不是。**

**顺带得到一个真有用的量**:`candidate_aten_ops`(未融合掉的 aten 算子个数)。全融合冠军 **13**,把两个 1×1 卷积交回 cuDNN 的混合候选 **33** —— 它测的是**融合程度**,这是它自己能回答的问题(并且是 G15 的替代信号)。

**状态**:✅ **已修 `9e538b9`**(说明串 + 候选级替代量;候选总 FLOP 已实测证明 aten 路不可行,原因记录在案)。

---

### G3 "资源→性能"没有强制兑现 —— 方向错了

**用户原话**:"v3 里的效率不仅是指资源之间的转换效率,也指资源和性能之间的转化效率(如果单纯资源间转换而性能没有提升,那完全没有意义)"。

**这条纠正的是目标函数的层级。** 我的文档里定义了 `转化效率 = Δ耗时 / Δ资源`(这确实是资源→性能),但**另外还写了"资源之间的替代率"(用容量换流量),而那部分没有要求兑现成性能**。

**为什么严重**:一个改动可以让共享内存降 40%、寄存器降 30 个、占用率翻倍,而**耗时一点不动** —— 按"资源间替代率"记法它是成功的,实际毫无价值。而这不是假想:今天实测 **23 次超噪声底的成功改写里,11 次(约一半)没有缓解任何绑定的资源维度**,却确实变快了;反向的情形(资源改善而耗时不动)必然也存在,而框架**没有任何机制把它标出来**。

**修法(三条,改的是层级不是指标)**:
1. **唯一最终判据是耗时。** 资源分析是**手段**,不是目标。
2. **每次资源替代都必须报告兑现了多少性能**:"共享内存 +2048 字节 → 耗时 −0.3 ms(超噪声底)",或者"资源变了而耗时没动 = **本次替代无效**"。
3. **新增输出类别「资源改善但性能没动」**。这类结果现在被当中性丢掉,其实**最有信息量** —— 它直接证明那个资源不是当前瓶颈,比任何利用率标签都可靠。
4. **"资源→性能转化率"本身要被追踪**:同一维度上的历史记录("这个任务上共享内存每多用 1 KB 平均换来多少 ms")才是能引导下次改写的东西。

**状态**:✅ **已实现**(`src/kernel_optimizer/evaluation/conversion.py`,接进 `FAMILY_ROUND_RECORDED`)。

**落地形态**:每个改写轮次现在都在事件日志里记录一个 `conversion` 判决:

| 判决 | 含义 |
|---|---|
| `improved` | 耗时下降超过噪声底 —— 附带列出同时改善了哪些资源 |
| **`no_conversion`** | **某个资源实质改善了而耗时没动** ⇒ **那个资源不是当前的限制**(最有信息量的一类) |
| `regressed` | 耗时上升 —— 任何资源改善都不能抵消 |
| `flat` | 耗时和资源都没有实质变化 |
| `unknown` | 前后耗时无法比较 |

**同时报告每个维度自己的 Δ**(前值、后值、变化量、相对变化、单位、方向),**并且不做跨维度排序**(遵守 G8 的纪律)。占用率的极性单独声明(高才算好),并有专门的反验证测试 —— 把它写反不会报错,只会给出一张干净的错表。

**反向验证**(两项,都实测会失败):(a) 把占用率极性写反 ⇒ 测试捕获;(b) 让"仅资源改善"返回 `improved` ⇒ 测试捕获并打印出那条断言。

---

### G4 显存峰值维度缺失 —— 已实测可行

**先前状态**:全仓库零 `max_memory_allocated` / `peak_memory` 引用(已复验)。

**实测(box 1,`scripts/probe_per_candidate_cost.py`,2.2–4.3 秒/候选)**:

| 任务 | 显存峰值区间 | 跨候选跨度 |
|---|---|---|
| L3:48 | 1288 – 1424 MiB | **9.55%** |
| L3:21 | 1206 – 2055 MiB | **41.3%** |

**两个任务上冠军都是显存峰值最低的那个。** 与耗时的秩相关 Kendall **τ=+0.64**(9 一致 / 2 相反,n=11 对)。

**必须标注的局限**:`max_memory_allocated` 是**缓存分配器**视角(分配器缓存的空闲块不计),不是真实设备显存。但**"会不会 OOM"问的正是 `max_memory_reserved`**,而"候选之间谁更省"问的正是 `allocated` —— 所以两个都记录。

**状态**:✅ **已修 `9e538b9`** —— `_measure_candidate_cost()` 已接进 worker,两条 profiler 路径都带上该字段(有专门测试断言**两条路都带**,因为只改一条是这类修复最常见的漏法)。**热身放在计时之前**,否则编译期的分配会进入峰值。

---

### G5 片上容量只当布尔判据 —— ✅ 已修

**先前的形态**:容量只通过 `at_limit` 字符串到达 agent —— **接近上限时**说 "shared=49664/101376",**没接近时什么都不说**。于是 agent 想问最基本的交易问题("我还剩多少共享内存可以拿去开更大的 tile?")在整份证据里找不到答案,而"花容量买复用"恰恰是最常见的结构性动作之一(L3:43 第二轮改写就是手算 73728B→40960B 来解锁更大 tile)。

**修复**:新增四个字段,**报给每一个判决而不只是 `resource_limited`**:

| 字段 | 含义 |
|---|---|
| `shared_headroom_bytes` | 还剩多少共享内存可花(绝对字节) |
| `shared_used_frac` | 已用比例 |
| `reg_headroom_per_thread` | 每线程还剩多少寄存器 |
| `reg_used_frac` | 已用比例 |

**绝对量和比例都报**:比例本身没法换算成 tile 尺寸。

**修复时被自己的测试抓到一个真实放置错误**:我最初把这段代码放在函数末尾的 `near_limit` 块附近,而 `memory_bound` 在到达那里之前就 return 了 —— **恰好漏掉最需要这个数的那类判决**(带宽受限时"花容量买复用"正是该做的动作)。测试直接 `KeyError` 失败,已移到所有早退分支之前。

**边界情况**:超限读数 clamp 到 0 而不报负数("你可以花 −2048 字节"读不通);**未测量则字段缺席**,不能由缺失读数推出"容量全剩"从而诱导 agent 去花它没有证据的容量。

**状态**:✅ 已修(box 1 399 passed / 0 failed)。

---

### G6 候选真实 DRAM 流量测不到 —— 已有下界+上界

**三条旧路仍然不通**(已在代码里逐一证实):数汇编指令是静态文本计数(K 循环 100 次的 GEMM 其读指令可能只出现 2–3 条);aten 级拦截**融合越好越低报**;现有 profile 无 grid/启动次数。硬件计数器被 `ERR_NVGPUCTRPERM` 永久封死。

**但"完全没有"过于悲观。实测拿到了两个界**:

| 记号 | 方法 | 是什么 | L3:21 跨候选跨度 |
|---|---|---|---|
| **下界** | `__torch_dispatch__` 拦截**候选** | aten 层物化流量。**对融合进 kernel 内部的搬运瞎** ⇒ 低报 | **81.9%**(990 – 5465 MiB) |
| **上界** | `task_cost.reference_bytes` | 参考物化的全部流量(含留在 L2 的中间量) ⇒ 高报 | 任务级 |
| **启动几何** | 包装 `JITFunction.run` | blocks × warps × 32。**实测 27/27 次启动的 grid 都是真元组**,拿得到 | 16.3%–75% |

`候选下界 ≤ 真实流量 ≤ 参考上界`。与耗时秩相关 **τ=+0.82**(10 一致 / 1 相反)。

**注意方向相反**:`reference_bytes` 的局限是**上界**(把 L2 里的也算上),候选 aten 流量的局限是**下界**(把融合掉的漏掉)。两个界一起用才有意义。

**状态**:🟡 **已修 `9e538b9`**(两个界都落地,跨候选可比,τ=+0.82);**精确值仍不可测** —— 需硬件计数器,而 `ncu` 被 `ERR_NVGPUCTRPERM` 永久封死。这条**永远停在 🟡**,不是待办。

---

### G7 L2 命中被算成 DRAM 流量 —— ✅ 已修

正对照实测:5 个按构造已知瓶颈的 kernel,判据**误判 2 个**;一个数据全在 L2 内的流式 kernel 读出 DRAM 屋顶的 **287%**(物理不可能)。

**今天休眠**:三个真实任务的工作集全部远超 72 MiB L2(L3:21 307 MiB / L3:43 397 MiB / L3:48 1.351 GB),所以当前零污染。**扩大任务范围(level1/2 小任务)前必须修。**

**两条修法(都便宜且泛化)**:(a) 压强 > 1.05 报 `unknown` 并注明"疑似 L2 命中" —— 超限说明**分母不适用**,比 clamp 更强;(b) 工作集 vs L2 容量作为该维度 `applicable` 的前置。

**状态**:✅ **已修 `9e538b9`** —— (a) 已落地:`_IMPOSSIBLE_FRAC = 1.05`,超限返回 `kind="unknown"` 并带 `impossible_dram_fraction` 与一条"改用 L2 命名"的建议。**不 clamp** —— clamp 会保留"已饱和,别再优化"这个错误动作,而这正是危害所在。(b) 未做:需要每候选的工作集,而那要先有 G6 的精确值。

---

### G8 禁止跨维度比"谁变化最大" —— 已登记为纪律

23 次超噪声底成功改写上实测的**摆幅**:共享内存中位 **0.162**、寄存器中位 **0.012**、占用率中位 **0.000**。共享内存的中位摆幅是寄存器的 **13.5 倍**,纯粹因为归一化分母不同(`shared_bytes/101376` 一改 tile 就动几万字节)。

**造成的实际错误**:重新打分后"最佳对照"变成"永远挑共享内存"= 61.5%,它什么都没发现,只是叫出了摆得最宽那一维的名字。

**纪律**:禁止用跨维度 `argmax |Δ|` 做(a)打标签、(b)排序、(c)报告给 agent。**替代:每个维度只和自己的历史分布比。**

**状态**:✅ 已登记(A-3b),且 S2c 的资源地图本来就走原始量、只在同维度内比较,不受影响。

---

### G9 给 AI 更多信息不保证更好 —— 只能实验

**外部反向先例**:KernelBench 论文实测 few-shot 优化范例**降低** fast_1(o1 在 level1 上 10%→6%),因为模型"attempt more aggressive optimization strategies"而失败更多。

**这是整个 v3 最大的风险** —— v3 的全部价值在于给 agent 更多信息。**无法靠设计消除,只能靠实验**:判据是"终局不变差",退路是回退消化层的措辞而不回退数据采集。

**状态**:🔧 **实验进行中**(box 1,2026-09-10 启动)。

**实施方式**(`scripts/g9_information_ab.py`,按要求用**真实算子 + 真实 prompt**,不跑完整 task):

- **起点**是 L3:48 已完成 run 里实测 **1.5544 ms** 的冠军候选(`cand-efcecff9`,`DOT_PRECISION=fp16, BLOCK_P=64`),配它真实的 analyst 报告与 profile;
- **唯一变量**是 rewriter 拿到的证据,其余(任务、起始源码、prompt 模板、模型 `zhipuai/glm-5.3`、沙箱布局)全部固定;
- **三臂**:`none` 同 schema 但无分析(对应 KernelBench 的 NoFeedback 对照) · `verdict` v3 现状 · `rich` 再加 G4/G6 的候选级实测量;
- **每个产出的 kernel 都过 harness 自己的 `quick_test`**(编译 + 正确性 + 计时),所以结论是**测出来的**,不是读代码判断的。

**两个设计要点**:

1. **`rich` 必须是 `verdict` 的严格超集** —— 否则差异可能来自「减了什么」而不是「加了什么」。
2. **加了一道拒绝运行的守卫**:提取到的 profile 早于 G4/G6,本来一个新字段都没有,`rich` 会**静默等同于** `verdict` —— 两个什么都不差的臂被当成做过比较报出来,正是本项目反复踩的「可信的空操作」。已先在该候选上实测出真值(peak_alloc 1288 MiB / aten_bytes 1024 MiB / aten_ops 1 / threads 4194304)才允许开跑。

**样本量的诚实说明**(见 G20):3 臂 × 2 次 = 6 次真实调用。**2 次/臂是轶事,不是比率** —— 这些任务每 trial 标准差可达均值 16%、近平局跨度 5–9%,所以它**只能排除巨大差异,不能证明小差异**。脚本会自动打印这句话。

#### 首份结果(2026-09-10,box 1,`scripts/g9_recover_objective.py` 从磁盘复原)

先说一件必须先说的事:**A/B 脚本自己把实测量读丢了**(`ms: null`,见 **G22**),6 个候选的耗时其实一直好好躺在 run 自己的 `jobs/*.out.json` 里。所以下表是从 worker 的原始结果文件复原的,**没有重跑任何一次 agent 调用,也没有重测任何一个 kernel**。

| arm | 调用数 | 产出 | 正确 | **best_ms** | 每次调用最好值的均值 | agent_s | cost |
|---|---|---|---|---|---|---|---|
| `none` | 2 | 3 | 3 | 1.5770 | 2.0731 | 1500 | 0.0000 |
| `verdict` | 2 | 2 | 2 | **1.5575** | 1.9172 | 1500 | 0.0000 |
| `rich` | 2 | 4 | 3 | 1.5616 | **1.5739** | 1344 | 0.0277 |

**结论:没有任何一臂分离。** 三臂最好成绩的跨度是 **1.25%**,而这个任务记录在案的近平局带是 **5–9%**、每 trial 标准差可达均值的 **16%** —— 1.25% **在噪声底以内**,不能读成臂间差异。

**三条必须一起读的限制**:

1. **n=2 次调用/臂**,是**轶事**不是比率。
2. **起点已经是 1.5544 ms 的冠军**,而三臂的 best 全在 1.5575–1.5770 —— **没有一臂改进了起点**。L3:48 已被三次独立 run 判定为**物理平台期**(93.7–95.5% DRAM 屋顶)。**在平台期上做信息 A/B,本身就是最不可能看出差异的场地** —— 这是实验设计的缺陷,不是结论。
3. **存在混淆因素**:**5/6** 调用死在 1500 s 传输天花板(**G20**),候选是从沙箱救回的,`change_summary` 是占位文本,所以三臂之间**除了「被告知什么」还差着「agent 自己的总结有没有活下来」**。

#### 重跑(2026-09-10 17:2x,box 3 / A800)—— 三处改动,预计 2.41 h → **0.33–0.42 h**

首次实验的三个问题各对一处改动,**没有一处削弱实验本身**:

| 问题 | 改动 | 依据 |
|---|---|---|
| **场地在物理平台期** | 换到 **L3:21** | `scripts/g9_pick_venue.py` 按实测选场地:L3:21 顶部跨度 **31.69%**,L3:48 那个 run 只有 **1.74%** —— **18 倍的空间**。判据是「顶部候选之间还有多少差别」,平台期的指纹就是顶部挤成一团 |
| **墙钟 2.41 h,而 GPU 全程空闲** | `--concurrency 6`,**6 路并行** | agent 调用是纯网络等待(中位 18.9 min GPU 空转)。每个调用本来就有自己的 session 和沙箱 ⇒ 并行**不改变任何一个调用收到什么**。GPU 评测仍**串行**(phase 2),因为同时给两个 kernel 计时会同时污染两份测量 |
| **G20 修复没生效** | 改到 **box 3** 跑 | **box 1 的 checkout 在 `062f8e8`,`runtime.py` 的 G20 符号数为 0** —— 那台机上没有这份代码。box 3 在 `f2d4835`,12 个符号。所以 750 s 静默中止会真正参与 |

**并行的唯一真实风险已处理**:`OpencodeClient._abort_and_close` 会**关闭 httpx 传输**(必须关——单靠 abort 会让流式 POST 永久挂住)。若多线程共用一个 client,**一个调用触发空闲中止就会把其他在飞调用的连接池一起抽掉**,那些调用会因与自己无关的原因失败 —— 在 A/B 里就是**一个臂被另一个臂的超时冤枉**。所以**每个 worker 一个自己的 client**,`tests/test_g9_parallel_arms.py` 4 个测试守住这条(含「若 `_abort_and_close` 哪天不再关传输,就大声失败提醒重新评估」的反向守卫)。

**`rich` 臂已确认非空**:L3:21 起点的 profile 原本 5 个 per-candidate 字段全为 None(**守卫会拒绝开跑** —— 否则 `rich` 静默等同 `verdict`)。已用 `scripts/g9_measure_rich_fields.py` 在**两台机上分别实测**,结果完全一致(peak_alloc 1206.1 MiB / aten 989.8 MiB / 13 ops / 14,805,120 threads)—— 这些是分配量与流量,不随时钟变,一致正是预期。



---

### G10 后端天花板 —— ✅ 已修(实测同时否证了外部数字,并发现缺陷是双向的)

**问题**:标定用 `torch.matmul`(即 cuBLAS)测四个精度的算力天花板,而**我们生成的每一个候选都是 Triton**。外部实测称 V100 张量核经 cuBLAS 达理论峰值 **96.5%**、经 WMMA 仅 **54%**,而 Triton 发的就是 WMMA。

**那个 54% 不能直接引用**(不同卡、不同代、手写 WMMA 而非 Triton codegen),所以写探针实测。**结论:外部数字在这张卡上被否证,而真正的缺陷比它描述的更糟 —— 是双向的。**

**box 1 实测(72 配置扫,每个数都过 fp64 正确性门)**:

| 精度 | cuBLAS | Triton | 比值 | 相对误差 | 用 cuBLAS 当天花板的后果 |
|---|---|---|---|---|---|
| fp32/ieee | 54.20 | 45.61 | **0.841** | 1.62e-06 | 屋顶**够不到** ⇒ 凭空多出 16% 余量,而任何 tile 改动都到不了 |
| tf32 | 88.14 | 86.58 | 0.982 | 7.91e-04 | 基本一致 |
| fp16 | 159.47 | **174.78** | **1.096** | 2.07e-04 | 屋顶**是个反天花板** ⇒ 比例超 100%,下游读成"已饱和,别优化" |
| bf16 | 162.29 | **175.18** | **1.079** | 1.66e-03 | 同上 |

**两个方向是同一个错误:把某一个库的成绩当成物理屋顶。**

**修法(泛化,不含任何硬编码比值)**:天花板 = **本机实测的所有路径取最大值**,并把**分后端的数字并列报告**,这样"够不到的屋顶"是显式可见的,而不是默默要求做不到的事。

**落地**:新增 `gpu/tritonmm.py`(用**朴素分块 Triton matmul** —— agent 会写的形状,不是调优过的库),接进标定(schema **2→3**,所以旧缓存会被重测而不是永久供 0.0)、分类器、以及 agent 的天花板提示块。

**GPU 端到端复验**(`scripts/probe_calibration_triton_ceiling.py`,走的是标定真正会调用的那条路):**4/4 精度测出、0 个配置因算错被丢弃**,方向与 72 配置探针一致(0.843 / 0.990 / 1.080 / 1.039)。**标定用的 6 配置精简扫略微低估 Triton 上限**(fp16 1.080 vs 1.096),所以落地的天花板**偏保守**而非偏乐观 —— 这个方向是安全的。

**新增证据字段**:`backend_reachable_frac` + `backend_reachable_basis`。**为什么必须单独报**:"你到了屋顶的 84%" 和 "84% 就是你这个后端的上限" 指向**相反的动作**(继续调 vs 换代码生成器),而没有这个数时两者无法区分。

**探针本身先被抓到三个缺陷,全是"编得过、跑得出数、数是错的"**:

| # | 缺陷 | 症状 | 修法 |
|---|---|---|---|
| 1 | `tl.dot` 在 fp32 输入上**默认走 tf32** | fp32 比值读出 **1.584** = "Triton 比 cuBLAS 快 58%",实际两边精度不同 | `input_precision` 作为 constexpr 钉成 `"ieee"` |
| 2 | **探针从不检查 kernel 算得对不对** | 3/4 精度读出 Triton 高于 cuBLAS,而**算得少的 kernel 一定更快** | 每个配置的输出与 **fp64 参考**比相对误差,超容差**丢弃该配置** |
| 3 | kernel 内用 Python 内置 `min` 对 tensor 求值 | 编得过、出数 | 改 `tl.minimum` |

第 2 条正是清单里已有的那条纪律([[probe-needs-a-positive-control]]):**阴性/异常结论必须有会大声失败的正对照**。加了门之后**四个精度全过**(0 个配置被丢),所以那两个 >1.0 的比值**是真的** —— Triton 在这张卡的 fp16/bf16 上确实赢过 cuBLAS。

**修完之后我自己的两个测试又失败了,两次都是真 bug 不是测试写坏**:

| # | bug | 症状 |
|---|---|---|
| 1 | 可达比例按**传入的**精度取,而不是**实际生效的**天花板 | tf32 的可达比例印在 fp32 的百分比旁边 —— 一份判决里两个屋顶 |
| 2 | 用 `< 0.98` 抑制"太小的差距" | 实测 tf32 就是 **0.982** ⇒ **恰好把最多候选使用的那个精度的差距藏掉了** |

第 2 条已把阈值从证据路径**整个删掉**(任何阈值都是在猜哪些差距重要);只有 agent 提示保留 5% 带并写明理由(提示是给人读的散文,四行近似相同的数字会埋掉真正重要的两行)。

**状态**:✅ 已修 `a2f32e6`,box 1 **399 passed / 0 failed**。

---

### G11 / G12 / G13 / G14 / G15

- **G11 运行期发现维度 ✅ 已修(文档)**:实际能做到的是"**预置维度集合 + 逐维判定 applicable**",**不是**自动发现一个未预置的新维度 —— 那个机制从未设计。`v3-design-resource-ratio-and-conversion-efficiency.md` 里有三处把它写成"维度集合运行期发现",已全部收窄,其中包括**新颖性主张那一句** —— 新颖性不能建立在一个我们没有实现机制的能力上,那是审稿时最容易被击穿的一类表述。保留的主张改为"每个维度自带 `applicable` 与 `ceiling_provenance`",这是真正实现了的部分。
- **G12 异构卡比较 🟡 已解锁(A800 到手)**:候选是"只比两卡都适用的维度并显式报告被排除的",或"不比数值而比判决模式"。**现在有了一个新约束**:G5 落地的 `*_used_frac` 是**对硬架构上限的比例**(255 寄存器 / 101376 字节),这类比例**跨卡本来就可比**,不需要合成 —— 所以异构比较可以先从这几个维度做起,把真正难的(带宽/算力这种需要屋顶的)留到有 A100 实机时再定。**A100 测试的前置项,现在不阻塞任何东西。** 【2026-09-10 更新】**A800 80GB 已配好,第一份跨卡实测到手,而它直接给了 G12 一个判据**:Triton/cuBLAS 的比值在两张卡上**反向** —— 4090 fp16 **1.096** vs A800 fp16 **0.808**;4090 tf32 0.982 vs A800 tf32 **1.032**。所以「某后端能达到峰值百分之几」这个数**本身不可跨卡比**,它是(卡, 精度)这一对的属性。可比的是**判决模式**(哪一维绑定)与**对硬架构上限的比例**(`*_used_frac`) —— 与本条原先的两个候选方向一致,现在有实测支持而不只是猜想。
- **G13 替代自检 ✅ 已实现**(`src/kernel_optimizer/evaluation/reading_checks.py`,接进 `LightProfiler`)。撤回 TMA 闭合后失去了 `Σ分量 ≤ total` 这个免费自检,两条替代都已落地,且**都是对一次真实故障的泛化** —— 它们的共同点是**故障不报错,只给出一张可信的错表**:
  - **跨配置恒为常数** ⇒ 判为采集器故障。来自实测:某探针在 108 个不同配置上把 `n_regs` 全读成 **56** —— 56 是个完全正常的寄存器数、没有任何字段是 None、没有异常,所以"非 None"检查抓不住。**少于 3 个读数时拒绝判断**(两个相同不值得报警,否则每个小 sweep 都会误报)。
  - **超出可能范围** ⇒ 判为该读数不来自硬件。来自实测:fp16 kernel 拿 tf32 屋顶当分母读出 **107.8%**,下游读成"已饱和,别再优化"。**报告而不 clamp** —— clamp 会保留那个错误动作。
  - **只标注,绝不否决**:能让 run 失败的自检会把"采集器坏了"报成"候选坏了",而这正是这批修复要防的反转。
  - 另:每个维度显式声明极性这条已在 G3 的 `conversion.py` 里落地(占用率高才算好,并有专门的反验证测试)。
- **G14 通信/多卡 ⏳ 明确不做**:需要多卡环境,当前两台机都是单卡 4090。**记录为范围外而不是缺陷** —— 把一个没有硬件可测的维度留在缺口清单里,会让清单长度显得比真实待办多。
- **G15 谓词化维度 ❌ 硬件层面不可行,但有一个便宜的替代信号**:抓死分支事故(`if bn.training:` 让 31 个 trial 全测 fallback、零报错、却是全 run 最差)需要 warp-vs-thread 指令计数 ⇒ **被计数器权限永久挡住**,cubin 能看到分支结构但看不到活跃线程数。**替代:G1/G2 修复顺带给了一个** —— `candidate_aten_ops` 与 `threads_launched` 现在按候选实测,而一个把优化关在死分支后面的候选会**落回 aten 实现**,于是 `candidate_aten_ops` 会显著偏高(实测那个把两个 1×1 卷积交回 cuDNN 的候选是 **33** ops,而全融合的冠军是 **13**)。**这不等于测到了谓词化,但它会在同一类事故上响** —— 待在真实死分支案例上验证。

---

### G18 Linux 移植代码不在分支里 —— 缺失(✅ 已修 `44de7e7`)

**先前状态**:三个文件只以**未提交**状态存在于 box 1 的工作树(149 行插入),v3 分支的新克隆在任何 Linux 机上**完全跑不起来**(`worker_client.py` 会去调 `wsl.exe` 并把路径翻成 `/mnt/d/...`)。所有实验结果都产自那三个未提交的文件 ⇒ **任何人按分支复现都会失败。**

**读完 diff 的关键发现:大部分改动根本不是平台差异 —— 有三个是 Windows 上也存在的 bug**,只因为它们混在一个「Linux 移植」里而被困在一台机器上:

| # | bug | Windows 上也错 | 后果 |
|---|---|---|---|
| 1 | `pkill -f worker_main.py` **匹配全机所有 worker** | **是** | `max_shared_jobs=2` 时,一个 job 超时会**连带杀掉另一个健康 job**,后者报 `worker_crash` ⇒ **一次真实失败被归因到无关的候选身上** |
| 2 | `~` 从不被展开 | **是** | 配置默认值用 `~`;`bash -lc` 在 WSL 路上恰好帮它展开了,原生路上没人展开,字面 `./~/...` 会让**每个 job 都失败** |
| 3 | `stop()` 调 `taskkill`,POSIX 上抛 OSError,回落的 `proc.kill()` 只杀直接子进程 | **是** | 在 `shell=True` 下那个子进程是 `/bin/sh`,**真正的 opencode server 变成孤儿** ⇒ 每个 run 泄漏一个进程 + 占住一个端口 |

**真正需要按平台分叉的只有三处**,已用一个运行时判断 `_wsl_hop_needed()` 收口:

- **路径翻译** —— Windows 翻,Linux 恒等;
- **`wsl.exe ... bash -lc` 包装** —— 仅 WSL 路需要,且**只有这条路需要引号转义**(原生路走 argv list,所以**路径含空格再也不会崩**);
- **`shell=True`** —— Windows **必须**(opencode 是 `.cmd` shim),POSIX 上**是个 bug**:`shell=True` 配 list argv 会让 `--hostname/--port` **被静默丢弃**,服务器绑到默认端口,健康检查随后超时,**症状看起来完全像网络故障**。

`doctor` 现在**恒查 `nvidia-smi`**(两种拓扑真正的前置条件,旧代码从来没查),**仅在 WSL 路上查 distro**。

**实测**:A800 **422 passed / 0 failed**(`test_to_wsl_path` 在那里从失败变通过);Windows 上受影响的四个测试文件 **22 passed**。

**我自己的三个新测试先失败了,三次都是测试的错不是代码的错**:两次是源码文本搜索把「解释旧代码为什么错」的 docstring 当成了缺陷本身(**断言源码字符串会在正确代码上失败**);一次是伪造了 pid 却没伪造 `getpgid`,于是它**测的是回落路径却以为测的是主路径**。

---

### G19 给 agent 的散文里写死了硬件比值 —— 算错了(✅ 已修 `44de7e7`)

**你的原则是对的,框架也确实没有硬编码数值 —— 漏的是"比值"。** 所有硬件**数值**一直是每台机实测的(这部分设计正确),但**给 agent 读的散文里,数值之间的比值被写成了常数**,而且换卡时没人更新。

| 比值 | 4090 实测 | A800 实测 | 散文里写的 | 错多少 |
|---|---|---|---|---|
| fp16 / tf32 | 1.80 | 2.05 | 「~2x」 | 侥幸两边都还行 |
| **tf32 / fp32** | 1.61 | **5.88** | 「1.6x」 | **A800 上错 3.7 倍** |
| **fp16 / fp32** | 2.88 | **12.05** | 「~2x」 | **A800 上错 6 倍** |

**为什么这个错误特别有害**:A800 的 fp32 只有 19.0 TFLOP/s(比 4090 还慢 2.9 倍),**它恰恰是最需要离开 ieee 路径的卡**,却被散文告知这个动作只值 1.6 倍(真实 5.9 倍)。这会直接改变 rewriter 的优先级 —— 它可能去调寄存器/共享内存,而不是做那个值 5.9 倍的精度切换。**同一个量有两个来源(一个实测、一个过时),agent 无从判断该信任哪个。**

**修复**:删掉 5 处写死的比值(prompt 里 3 处 + `modules.py` 散文 2 处),**散文只描述机制,数字一律由 `_measured_ceilings_doc()` 用两个实测天花板现算注入**。

**加了防回归测试**:扫 prompt 与 prompt 构造代码,发现「speed 语境 + 裸倍数」就失败。**这类硬编码正是靠「举个具体例子更好懂」长出来的**,所以需要一道机械的门。测试同时**排除掉政策常数**(10x 反作弊阈值是本项目自己选的规则、每张卡都一样,不是关于硬件的断言)—— 这是收紧判据而不是放宽它。

**反向验证**:同一段代码在 A800 标定上必须产出 `5.88x`、在 4090 标定上必须产出 `1.61x`,且**A800 的比值不得出现在 4090 的文档里** —— 证明它是算出来的,不是恰好匹配某一张卡的常数。

---

### G20 每次 rewriter 调用中位 18.9 分钟 —— 缺失(✅ 已修 `44de7e7`)

**实测(n=9,已完成 L3 run)**:中位 **18.9 min**、均值 19.0、区间 9.5–30.0、四分位 13.3/18.9/25.0,**33% ≥24 分钟即撞上超时天花板**。

**为什么不能直接抬高上限**(你问的):

1. **超时是症状不是原因。** 记录在案的根因是 agent **自己写了一个 1500+ 次编译的穷举自检**,吃满 1800s、零产出。给它 3600s,它会写个更大的自检然后吃满 3600s。
2. **成本线性放大。** `max_transport_retries=2` ⇒ 三次尝试 ⇒ 一次挂死的调用要烧 **90 分钟**;上限翻倍就是 180 分钟。
3. **对样本量是反方向的。** 实验缺的是**更多样本**,抬上限让**每个样本更慢**。

**修法(中期方案,已实施)**:保留原则「光是跑得久绝不能结束一个调用」(在编译、在跑 benchmark 的 agent 是在工作),**补上它的逆命题:一直不产出就该结束**。产出信号取 **agent 沙箱的文件树**(文件数 + 总字节 + 最新 mtime)—— agent 工作时它一直在变,挂住时它不动。

**阈值按你的要求是推导出来的,不是写死的 20 分钟**:`idle_abort_frac`(默认 0.5)× `request_timeout_s`。所以**抬高超时上限会自动放宽静默容忍度,缩短上限也会自动收紧**,两者不可能各自漂移。另有下限 60s,防止在很短的上限上算出一个会误杀正常调用的窗口。

**三条防误杀的设计**:
- **沙箱读不到 ⇒ 判为「无法判断」,绝不判为挂住** —— 否则一个读不到的目录看起来和挂死的 agent 一模一样,会把**诊断故障报成 agent 故障**(这正是这批修复要防的反转);
- **文件变大也算产出** —— 只数文件个数会把「原地改写一个 kernel」误判成静默;
- **内存看门狗原样保留** —— 那是「快把机器搞挂了」,与「静默」是两个不同的条件,不能互相替代(有测试专门守这一条)。

**顺带记录一条正面事实**:G9 已有 2 次调用撞满超时,**两次 artifact rescue 都成功救回了已写好的 kernel**,两个臂都没丢 —— 这条既有修复在实战中生效了。

#### ⚠️ 实测:修复没有让运行中的实验受益(状态因此下调为 🟡)

G9 跑完后复核,**4 次完成的调用里有 3 次仍然死在 1500 s**(1500.5 / 1500.6 / 1500.4 s,全部 `prompt transport error (ReadTimeout)`),只有 1 次(`rich` 臂)在 1187 s 正常返回。

**原因不是修复无效,而是修复没被加载**:那个实验的进程在修复提交**之前**就启动了,而空闲中止逻辑在 **driver 侧**(`OpencodeClient`)。这正是本项目已经记录过的分界——**worker 侧的修复对运行中的实验立即生效,driver 侧的不可能**。所以判定一条修复有没有生效,必须按它所在那一侧的时钟定日期。

**两个真实后果**:
1. **G9 的样本量正是被这个吃掉的** —— 计划 6 次调用,2 小时只完成 4 次,其中 3 次把 25 分钟烧在静默上;
2. **臂之间因此多了一个混淆因素** —— 超时的调用其候选是从沙箱救回的,`change_summary` 是占位文本,所以三臂差的不只是「被告知什么」,还有「agent 自己的总结有没有活下来」。

**下一次以 driver 侧新进程启动的 run 才是这条修复的第一次真实检验。** 在那之前,G20 只能算「已实施、未验证」。


---

### G21 事件与元数据的字段路径全靠猜 —— 算错了(✅ 已修 `fb0f84f`)

**一天里踩了四次同一形态,每次都是"读成空结果"而不报错**,所以「我读错了」和「本来就没有」**长得完全一样**:

| # | 猜的 | 实际 | 症状 |
|---|---|---|---|
| 1 | `payload.candidate_id` | `payload.trial.candidate_id` | 候选 id 全 None |
| 2 | `latency_ms.median_ms` | `latency_ms.median` | **680 条 trial 读成 0 条** |
| 3 | `candidates/*.py` | `candidates/<id>/source.py` | 10 个源码读成「盘上没有」 |
| 4 | `compiled.shared` | `compiled.metadata.shared` | 探针报 **A800「Tier-1 信号失明」** |

**第 2 条的实测对照**(同一个真实 run):

| 读法 | 结果 |
|---|---|
| `payload.status == "complete"` | **0 条** |
| `payload.trial.status == "complete"` | **371 条** |

实际存在的延迟键是 `['max','mean','median','min','n_samples','samples','std']` —— **没有一个带 `_ms` 后缀**,`robust_ms` 是内存模型里的名字、日志里根本不存在。

**第 4 条最危险**:它的产物是**一个不存在的缺陷** —— 报「A800 的 Tier-1 资源信号失明」,而 **harness 自己的读取器工作得很好**。差一点被记进缺口清单。**这个方向比漏掉缺口更糟:凭空造出工作量,还让人对一台好机器失去信心。**

**暴露面实测**:75 个脚本手工解析 `events.jsonl`,45 个自己写 `["trial"]` 嵌套,32 个自己摸 `latency_ms`,共用读取层**不存在**。

**修法的本体不是封装,是「空结果抛异常」。** 新增 `store/read.py`:`completed_trials` / `trials_with_latency` / `best_trial` / `candidate_source` / `candidate_ids` / `events_of_type` / `kernel_metadata`。**一个照样返回 `[]` 的封装只是把同一个坑挪个位置** —— 四次错误全都是**空容器被当成答案**,所以「我预期可能是空的」必须变成调用方**写出来的决定**(`allow_empty=True`),而不是默认继承的沉默。报错信息带上**正确的字段路径**和**当时都有些什么**,所以撞上它的人学到的是对的东西。

`kernel_metadata()` **两个对象都找一遍每个字段**,只有**两处都没有**才报缺失 —— 这是唯一能支撑「这台机器失明」的依据,直接堵死第 4 类假警报。`shared == 0` 是真值(该 kernel 不用共享内存),与「没测到」严格区分。

**在三个真实 run 上验证**(`scripts/verify_reader_on_real_run.py`):

| run | 完成 trial | 最优 | 旧字段路径读出 |
|---|---|---|---|
| L3:48 | **371** | 1.5544 ms | **0** |
| L3:21 | **588** | 3.6050 ms | **0** |
| L3:43 | **165** | 7.2500 ms | **0** |

三个最优值与已记录的结果一致 ⇒ 读的是对的东西。另有 8 个单元测试,**每个复现一次原始故障**。

**老脚本按你的决定不迁移** —— 它们的结论已经落进文档,重跑没有价值,批量改动风险大于收益。**下次要引用某个脚本的结论时再迁移它**,那时正好需要确认它读对了。代价:本条的**实际收益要随迁移逐步兑现**,新代码从现在起受保护。

**⚠️ 本条当天就复发了,见 G22。** 复发这件事本身比原缺口更有信息量。

---

### G22 G21 在「修完之后新写的脚本」里立刻复发 —— 算错了(✅ 已修 `d66de53`)

**这是本轮最有说服力的一条,因为它证明 G21 的真正危险从来不是那 4 个旧调用点。**

**事实**:`scripts/g9_information_ab.py` 是在权威读取层 (`store/read.py`) **落地之后**写的,写它的人**当天刚花一整天修这个 bug**,结果那一行还是猜了三个名字:

```python
out["ms"] = lat.get("robust_ms") or lat.get("median_ms") or lat.get("mean_ms")
```

worker 存下来的键是 `{max, mean, median, min, n, samples, std}` —— **一个后缀都没有**。三个猜测全部读成 `None`。

**后果(实测,不是推演)**:

| | |
|---|---|
| 受影响候选 | **6 / 6**,全部 `ms: null` |
| 而实测量在哪 | **一直好好躺在** run 自己的 `jobs/g9-*-eval-*.out.json` 里,`median` 键下 |
| 直接后果 | **A/B 的每一臂目标值都是 null ⇒ 根本无法排序**,一个 2 小时、烧掉 4 次真实 agent 调用的实验读不出结论 |
| 实际损失 | **零**。测量早已付过钱,只有「报告」这一步把它丢了 —— `scripts/g9_recover_objective.py` 从磁盘全部救回,**没有重跑任何 agent 调用,没有重测任何 kernel** |

**根因**:G21 提供了正确的读取层,但**没有任何机制让人用它**。「提供一个正确的工具」和「让错误的写法不可能通过」是两件事,而本项目一再证明前者不够 —— 因为写错的人**恰恰是最了解这个坑的人**。

**修法**:
- A/B 改用 `store.read.latency_ms_of`(它知道真键,并按实测优先 median:n=20 时 median 排名正确率 93.2%,mean 只有 64.8%);
- **正确但没有耗时**,现在会明确报成 `no_latency_in_result:keys=[...]` 并把真实键列出来,而不是静静地记 null;
- `tests/test_g21_ab_objective_regression.py`:4 个测试,**断言真实磁盘形状**,并且**连 fixture 自己也守**(如果 fixture 长出了带后缀的键,测试会失败 —— 否则测试就会在坏代码上通过,正是「源码断言可以把 bug 编码进去」那个坑)。

**留下的教训(不改代码,记为纪律)**:凡是新写的、要从 run 里读数的脚本,**目标值必须走 `store/read.py`**;这一条不是风格问题,它决定实验能不能读出结论。

---

### G23 两台机的工具链不同 —— **非缺口,已按用户裁定关闭**

**发现于**给 A800 写 L3 config 时逐项核对两台机的环境。

| | box 1 | **box 3 (A800)** |
|---|---|---|
| 卡 | RTX 4090 sm_89 | A800 80GB sm_80 |
| **torch** | **2.9.1+cu129** | **2.8.0+cu128** |
| **triton** | **3.5.1** | **3.4.0** |
| venv | 两个(30 G 系统盘装不下两份 torch) | **一个**兼两用 |
| 数据盘 | 250 G | **50 G** |

#### 用户裁定(2026-09-10):这不是缺口

> 「triton/torch 的版本本身也是环境的一环(包括硬件等都是环境的一环),跨机无法归因是预期现象。」

**接受,并且这个框架比我原来的提法更正确。** 我把它登记为缺口时隐含了一个前提:存在一条「纯硬件差异」值得去分离出来。但**在这个项目里「环境」本来就是不可分的整体** —— 卡、驱动、CUDA、torch、triton、乃至容器,共同决定一个候选能不能编过、用多少寄存器、跑多快。挑出其中一层称为「真变量」、其余称为「污染」,是把一个人为的切分强加给一个整体。

这也和框架一直守的原则一致:**所有硬件相关数值都由框架在本机实测**(G19)。既然每台机都只用自己实测的数,那么「跨机无法归因到单一因素」根本不是缺陷,而是**这个设计的正确推论** —— 每台机报告的是「在这个环境里的真实表现」,那正是要测的东西。

**因此不做「同卡两工具链」隔离实验**(我原先提的修法)。它要回答的问题——「差异有多少来自卡」——不是本项目需要回答的问题。

**保留下来的是文档事实,不是待办**:两台机的完整环境差异已写进
`configs/experiments_l3_glm_a800.yaml`(NOTE A)和 `docs/box-a800-setup.md` §7.2。作用是**描述性的**:读跨机数字的人应当知道两个环境的完整构成,而不是被引导去把差异归因给某一层。

**对 G12 的实际影响:无。** G12 要的是「异构卡按维度比较的办法」,而各维度的量(寄存器余量、shared 余量、占用率、到各精度屋顶的距离)**每台机都是本机实测的**,本来就是「这个环境下的值」。G12 因此不需要工具链隔离才能成立。

---

### G24 G20 的产出信号误杀合法调用 —— 方向错了(✅ 已修)

**这是我自己加的 G20 修复造成的缺陷,而且只有在真正装了这份代码的机器上跑真实实验才会暴露。**

**实测(A800,run `g9-20260910-171936`)**:6 次 rewriter 调用里 **5 次在 12.7 min 被中止**,理由都是「25 files, unchanged」。

**那 25 个文件全是种子输入**:18 个 `.git` 钩子样例 + `opencode.json` + 6 个输入文档。**每个沙箱里都没有 `rewrites/` 目录** —— agent 一个字节都还没写。

**第 6 次调用是决定性反例**:同任务、同 prompt、同配置,它 **11.5 min 才写出第一个 kernel**,15.2 min 正常结束,产出 2 个有效候选。**也就是说那 5 次中止,发生在它们即将写出产物的一分钟前。**

**根因是我的前提错了**:我写的判据假设「在工作的 agent 会改动沙箱」。**对推理模型这是假的** —— glm-5.3 在服务端思考,不落任何本地文件,直到吐出答案。**光看文件,长时间思考与挂死完全无法区分。**

**修法(保留原则,换掉证据)**:「真的停了就该结束」这个原则不变,但判据从**单一信号**改为**两个信号的合取**:
- 沙箱指纹变了 → 有产出;
- **或者**服务端仍报告该 session 在工作 → 有产出;
- **只有两者都说没在动**才中止。

新增 `_session_is_working()`,三条设计要点:
1. **用自己的短命 transport**,不用 `self._http` —— 主传输此刻正阻塞在流式 POST 上;
2. **任何异常返回 `None`(无法判断),绝不返回 `False`** —— 与「沙箱读不到」同等对待。**探针故障不允许杀掉正常调用**,这正是本项目反复踩的反转;
3. 只在沙箱已静默整个窗口后才问一次,成本是每次中止决策一个请求,不是每次轮询。

**阈值仍然是推导的**(`request_timeout_s × idle_abort_frac`),没有引入任何硬编码分钟数。

---

### G25 被 abort 关掉的传输会沉掉下一个调用 —— 缺失(✅ 已修)

**同一次实测暴露的第二个缺陷**:3 个臂记录

```
RuntimeError: Cannot send a request, as the client has been closed.
```

`_abort_and_close` 必须关闭传输(否则流式 POST 永久挂住),abort 路径也确实会重建它 —— 但 abort 是在**看门狗线程**上抛的,可能落在调用方两次重试之间。httpx 于是抛**裸 `RuntimeError`**,**它不是 `AgentCallError`**,所以 `AgentModule` 的重试循环根本看不到它,调用直接丢失。

**修法**:在发请求前检查并自愈重开。放在 `prompt()` 入口而不是每个 abort 点,是为了**覆盖所有会关闭传输的路径(包括以后新增的)**,而不是依赖每一处都记得重建。

**一个连带修正**:第一版写成 `self._http.is_closed`,而既有测试注入的假传输没有这个属性 ⇒ 全套测试里多了一个失败。改为 `getattr(..., "is_closed", False)`:**一个会破坏注入传输的守卫,比它修的 bug 更糟**。

---

### G26 J2-8 仍未通过,且 A800 上比 4090 更糟 —— 算错了(⏳ S2 的硬前置)

**J2-8 是 S2 自己写下的硬门**:「L2 内的候选不得被报成 DRAM 绑定」。正对照 `scripts/probe_binding_criterion_control.py` 有 5 个已知答案的 kernel。

**2026-09-10 在 A800 实测:仍然 2/5 误判。**

| kernel | 内建的真实限制 | 判据给出 | |
|---|---|---|---|
| A stream | DRAM | dram 0.99 | ✅ |
| **B occupancy** | **占用率** | **top=compute** | ❌ **误判** |
| C compute | 算力 | compute 0.91 | ✅ |
| D shared | 共享内存 | shared 0.65 | ✅ |
| **E stream-in-L2** | **不是 DRAM** | **dram 1.04 ⇒ 读成 DRAM** | ❌ **误判** |

**A800 上更糟,而且原因正是硬件差异**:4090 上这个 L2 kernel 读出 **287%**,被 `_IMPOSSIBLE_FRAC = 1.05` 抓住;**A800 的 L2 只有 40 MiB(4090 是 72 MiB)**,同一个 kernel 读出 **1.04 —— 恰好从阈值下面溜过去**。

**这证明修法 (a) 单靠阈值不够。** 修法 (b)(工作集 vs L2 容量作 `applicable` 前置)**没有落地**:`calibration.py:174` 已经有 `l2_bytes` 字段,但 `bottleneck.py` **从不读它** —— 代码注释甚至写着「72 MiB L2」(4090 的数)并叫读者「自己去比工作集和 L2」,而它自己不比。

**这条是 S2 的硬门,不是 S2 之后的优化**:S2 的产物是**按维度并列的多份判决**,而**判据误判维度时,并列输出只是把同一个错误分成两栏**。所以它必须在 S2 编码前修掉。



---

## 三、结算

**23 条:19 条已关(含 G23 裁定为非缺口)、1 条部分(物理上限)、1 条重跑中、1 条已解锁未做完、1 条不可行。**

**本轮新增 G22,G23 经用户裁定关闭为非缺口,并把 G20、G21 从「已修」下调为「已修但…」**:

- **G20**:修复已进分支,但 **G9 首次实测 5/6 完成调用死在 1500 s** —— 根因已查明且比原先记录的更具体:**box 1 的 checkout 停在 `062f8e8`,`runtime.py` 里 G20 符号数为 0**,即那台机上根本没有这份代码,而不只是「进程加载的是旧代码」。G9 重跑已改到 **box 3(`f2d4835`,12 个 G20 符号)**,那才是这条修复的第一次真实检验。
- **G21**:权威读取层没错,但**当天就在新脚本里复发**(G22)。
- **G23**:**不是缺口。** 工具链与硬件同为「环境」的一部分,跨机无法归因是**预期现象**;详见 §G23。

| 结局 | 条 |
|---|---|
| ✅ 已修并有实测/必败测试 | G1 G2 G3 G4 G5 G7 G8 G10 G11 G13 G16 G17 **G18 G19 G22** (15) |
| ✅ 裁定为非缺口 | **G23**(环境不可分,跨机不可归因是预期) |
| 🟡 已修但实测暴露了残留问题 | **G20**(box 1 无此代码 ⇒ 重跑已移到 box 3) **G21**(当天复发 ⇒ G22) |
| 🟡 已修到可测的极限,精确值物理不可得 | G6 |
| 🟡 已解锁但未做完 | **G12**(A800 到手,已有第一份跨卡实测) |
| 🔧 重跑中(已换场地 + 已并行) | **G9**(L3:21 顶部跨度 31.69%,是 L3:48 的 18 倍;6 路并行,预计 0.33–0.42 h) |
| ⏳ 需硬件(多卡) | G14 |
| ❌ 不可行,已有替代信号 | G15 |

**代码改动累计**:15 个文件改、13 个新增(`evaluation/conversion.py`、`evaluation/reading_checks.py`、`gpu/tritonmm.py`、`store/read.py`、`configs/experiments_l3_glm_a800.yaml`、`scripts/probe_*`、`scripts/g9_*`(5 个)、`scripts/validate_box_config.py`、`scripts/verify_agent_env.py`)、**+45 个测试**。


**双平台实测**:**A800(原生 Linux)422 passed / 0 failed** · **Windows** 416 passed / 1 failed(`test_g10_backend_ceiling` 需要 torch,而 Windows host 按设计不装 torch —— 同一测试在 A800 上通过)。A800 上 `test_to_wsl_path` 从「在每台 Linux 机上必失败」变为通过 —— 分支代码现在真的能在 Linux 上跑。

**box 3 现已可跑真实 run**:一次真实 agent 调用穿过 harness 自己的 sandbox + permission + provider 路径,agent **自己跑了 `nvidia-smi`** 并读回 `NVIDIA A800 80GB PCIe`(9.8 s / $0.0023)。`scripts/validate_box_config.py` 对两台机的 config 全绿,且**负对照**(把 box 1 的 device 块拿到 A800 上验)会报出全部 4 处不符并退出 1 —— 所以它是**能判假的**,不是只会通过。


**每条修复都做了反向验证** —— 把修复反着写,测试必须失败。**测试一共抓住了我自己犯的七个错**:G5 的字段放在 `memory_bound` 早退之后(恰好漏掉最需要它的那类判决)· G10 探针缺正对照 · G10 的可达比例按传入精度而非生效天花板取(一份判决里两个屋顶)· G10 的 0.98 抑制阈值恰好藏掉实测的 0.982 · **G18 两次用源码文本搜索,把「解释旧代码为什么错」的 docstring 当成缺陷本身** · **G18 伪造了 pid 却没伪造 `getpgid`,于是测的是回落路径却以为测的是主路径**。

### 这批缺口的共同形态

**17 条里没有一条会抛异常。** 全部是"跑得通、出数、数是错的":

| 形态 | 实例 |
|---|---|
| **可信的常数** | 分子跨 0.36% 而耗时跨 4.6×(G1/G2);`n_regs` 在 108 个配置上全读 56 |
| **物理上不可能的读数被当真** | DRAM 屋顶 **287%**(G7);fp16 读出 **107.8%** 峰值 |
| **极性写反** | 占用率"高=绑定"报出 0 个多维绑定,而反例就在输入里 |
| **单点巧合当成交叉验证** | `n_spills` vs STL/LDL 在最小量级上撞对一次(G16),实测 5/5 差 4–33× |
| **没有正对照** | G10 探针读出 Triton 快过 cuBLAS,因为它从不检查算得对不对 |
| **把某个库的成绩当成物理屋顶** | G10:cuBLAS 在 fp32 高估 19%、在 fp16 **低于**实际可达 |
| **自己设的阈值恰好挡住自己的实测值** | G10 的 `< 0.98` 抑制 vs 实测 tf32 **0.982** |
| **数值实测但比值写死** | G19:天花板都是实测的,而它们之间的**比值**被写成散文常数,A800 上错 3.7–6 倍 |
| **杀伤范围过宽,把无关对象一起打掉** | G18:`pkill -f worker_main.py` 杀掉并发的健康 job ⇒ **真实失败被归因到无关候选** |
| **空结果被当成有效答案** | G21:680 条 trial 读成 0 条;探针**凭空造出**一个「A800 失明」的缺陷 |

**所以这批修复的统一手法不是加检查,是让每个量带上它的出处**(`*_basis` 说明串、`applicable`、`ceiling_provenance`、`backend_reachable_basis`、`cost_notes`),**并且只标注、绝不否决** —— 能让 run 失败的自检会把"采集器坏了"报成"候选坏了",而那正是要防的反转。

---

### G16 `n_spills` 与 SASS 的 STL/LDL 是两个不同的量 —— 算错了(✅ 已修)

**先前的断言**(`statics.py:13` 与 `bottleneck.py:20`):

> "spills VISIBLE as STL/LDL, **cross-validated** against Triton's own n_spills (STL=2/LDL=1 against n_spills=2, **agreeing exactly**)"

**这句是论文引用的"无计数器信号的双源交叉验证"依据,所以必须实测而不是靠推理。**

**box 1 实测**(`scripts/probe_n_spills_semantics.py`,5 个寄存器压强递增的 kernel,**全部访存宽度都是 `.32`**):

| n_regs | `n_spills` | STL+LDL | 比值 |
|---|---|---|---|
| 30 | 8 | 80 | 10.0× |
| 128 | 20 | 651 | 32.6× |
| 168 | 560 | 4025 | 7.2× |
| 168 | 1336 | 8531 | 6.4× |
| 168 | 2432 | 15250 | 6.3× |

**5/5 不符,差 4–33 倍,而且比值不是常数** —— 所以两者互相推不出来。

**原因从数字本身就能看出**:`n_spills` 是**局部内存的占用量**(`CU_FUNC_ATTRIBUTE_LOCAL_SIZE_BYTES / 4`,即 32 位槽数,且统计**全部**局部内存而不只是溢出);STL/LDL 是**访存次数**。**同一个槽在循环里会被反复读写**,所以访存次数随循环次数增长而占用量不变。

**原断言为什么会看起来成立**:它建立在**一个** kernel 上,而且是**可能的最小量级**(STL=2/LDL=1 vs n_spills=2)。在最小量级上任何两个小整数都容易巧合。

**修复**:两处 docstring 改为明确说明这是**两个不同的量、不可互证**,并附实测表。**两个量都继续采集** —— 它们是真正互补的两个视角:一个说局部内存**流量**多大,一个说局部内存**需求**多大。

---

### G17 反汇编器只在 CUDA toolkit 里找,而 Triton 自带一份 —— 缺失(✅ 已修)

`find_cuda_tool` 先查 PATH,再查 `/usr/local/cuda` 等 toolkit 目录。**box 1 实测 Triton 自己就带着这三个二进制**:

```
triton/backends/nvidia/bin/cuobjdump
triton/backends/nvidia/bin/nvdisasm
triton/backends/nvidia/bin/ptxas
```

**这是比 CUDA toolkit 更强的保证**:Triton 自己产出 SASS 时就调用它们,所以**只要 Triton 候选能编译,它们就一定存在** —— 而 toolkit 在很多容器镜像里是缺的,那种情况下整个 Tier 1 信号会静默消失(只留一条 `statics_note`,与"这个 kernel 没有张量核"长得一样)。

**修复**:在 PATH 之后、toolkit 之前查 Triton 的 `backends/nvidia/bin/`,路径从**已安装的 package 定位**而不是猜,所以跟着 venv 走。顺序保留 PATH 优先,这样运维刻意放在 PATH 上的特定版本仍然胜出。
